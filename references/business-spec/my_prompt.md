giờ tôi mới hiểu vấn đề nằm ở đâu. Tôi vừa nghĩ ra các bước làm tài liệu mà theo tôi nghĩ nó ổn hơn cách hiện tại:

- từ list tính năng sẽ cần AI lên danh sách các nghiệp vụ mà nhiều tính năng sẽ dùng chung (ví dụ nghiệp vụ số dư thì bên đặt lệnh, hủy lệnh, nạp/rút tiền, xem balance... sẽ đều dùng chung). Nếu Human duyệt danh sách nghiệp vụ mà AI đề xuất thì AI sẽ viết tài liệu cho từng nghiệp vụ này dưới dạng các file Markdown nhé. Nếu Human reject thì cần nêu lý do để AI update lại danh sách.
- ta sẽ luôn có 1 file kiểu như mục lục, nơi chứa đường dẫn các file nghiệp vụ và mô tả ngắn gọn nhưng đủ ý về nghiệp vụ và các chức năng nghiệp vụ trong từng file. Sau này AI dễ dàng hơn để tìm xem nghiệp vụ nào đó đã được làm chưa hay phải làm mới, thay vì phải đọc tên file và đoán hoặc phải đi đọc toàn bộ tất cả các file nghiệp vụ khi cần.
- một tài liệu nghiệp vụ luôn phải được Human review và approve thì mới tính là làm xong nhé.
- khi đã xong các file nghiệp vụ chung thì tới với nghiệp vụ riêng. Theo từng tính năng thì ta sẽ dùng AI phân tích xem cần làm rõ những gì, nhớ là vẫn dùng đệ quy như phía trên để hỏi rõ từng phần cho tới khi không còn gì mơ hồ nữa nhé. AI nhớ quét xem phần nào có rồi thì dùng chung nhé. Mỗi tính năng sẽ nằm trong 1 file nghiệp vụ chính, nó sẽ có luồng đi qua các bước nào, rồi các điều kiện liên quan...Cuối cùng muốn hoàn tất thì vẫn phải qua Human approve nhé.
- ở phần test thì thật sự tôi cũng không biết sẽ có những test gì, cần bạn gợi ý thêm cho chặt chẽ. Tôi mới chỉ nghĩ ra 1 cách test đó là với 1 nghiệp vụ stateless thì ta sẽ truyền tất cả các trường hợp có thể nghĩ ra của input + trường hợp biên, đặc biệt hoặc ở ranh giới các giá trị, sau đó check xem output có đúng ý không. Với nghiệp vụ statefull thì ta phải giả lập tất cả các trường hợp có thể có của các trạng thái (ví dụ test orderbook thì phải giả lập cả trường hợp bên mua/bán không có thanh khoản, hoặc thanh khoản mỏng...) sau đó ta kết hợp với các input như ở stateless, sau đó xây dựng tổ hợp đầy đủ của các trường hợp test, từ đó đảm bảo test hết mọi trường hợp có thể. Rồi còn test luồng, kiểu test tích hợp...Chỗ này cần bạn tư vấn kỹ. Test này là test kiểu nghiệp vụ ấy nhé, sau này test case cho code + hệ thống sẽ được sinh 1 phần từ đây.

Có vài lưu ý:

- khi tài liệu này link sang tài liệu khác thì cần chỉ rõ link vào mục nào của file tài liệu kia nhé, kiểu dưới dạng đường link luôn ấy chứ không phải nói bằng chữ đâu. Mục ở tài liệu được gọi vào cũng phải chú thích nó được gọi sang từ mục nào ở bên đó. Nếu bên file kia gọi sang toàn bộ nghiệp vụ bên file này (kiểu như tính năng đặt lệnh thì gọi sang toàn bộ nghiệp vụ file orderbook ấy) thì ta chú thích ở dưới cùng của file orderbook nhé. Từ đó ta tự có 1 bản đồ dễ dàng hình dung được nghiệp vụ này gọi đến những đâu và được những đâu gọi đến, để khi update thì có thể biết được chỗ nào ở các file khác cũng cần chú ý update tài liệu hoặc tests.
- khi ta update bất cứ mục nào luôn nhớ kiểm tra lại xem mục này đang gọi đến đâu và đang được ở đâu gọi đến, từ đó ta update đường link và mục chú thích dưới cùng của các file liên quan cho chuẩn.
- khi làm tài liệu mà phần nào đang chỉ có 1 nơi dùng, bỗng có 1 flow khác cũng cần dùng tới thì ta tách phần này ra làm thành phần nghiệp vụ chung nhé, xong update các phần tài liệu cần thiết phải gọi tới.



Tôi nãy nghĩ ra mà nhớ được có từng này thôi. Bạn đọc kỹ rồi tư vấn giúp tôi nhé. Cách này tôi thấy có vẻ nó thực tế hơn. Còn thiếu xót gì bạn cứ mạnh dạn đề xuất hoặc nêu ra nhé. Tiêu chí vẫn là đơn giản nhất có thể nhưng phải đảm bảo giải quyết tốt nhu cầu của ta, không được đơn giản đến mức sơ sài nhé