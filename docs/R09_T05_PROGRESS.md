# R09 T05 — outbox, cập nhật trực tiếp và phần vận hành còn lại

## Hiện hành — review r13 đã được Human duyệt

**Hiện hành, 2026-09-20.** Human đã duyệt đúng `R09-T04-OUTPUT-r1` revision 13 với digest trình duyệt `52927f9e36c11f5736ec1bcca005bdaacd502c6b3ae35173ef386d7dcf1475f2`. Public APPROVE trả `REVIEW_RECORDED`/`APPROVED`, byte verification đạt. Review sau duyệt có digest toàn file `2d1243ff12b6fd8ed13bb0dfd82f19c4a85f7731363b57f5aaa68a07bc076abc`, giữ 10 owner, 117 subject version và 154 input version. Public READ mới trả WAITING, đọc lại revision 13 `APPROVED`, đúng `W-009-ADMIN-OPS` IN_PROGRESS, không blocker và attention `IMPACT_REVIEW_REQUIRED`. Metadata Pilot commit local `993b7e81c66045fc8ef60b21d2cd216b2c9b667f`, sạch, không remote/pending. [Bằng chứng](../tests/evidence/r09/recovery-protocol-v3-r1/summary.json).

Kidea implementation vẫn là commit/push `0b7fa924`: protocol v3 dùng integrity-bound input cho review write mới, hỗ trợ ngược v2 và giữ trần 128 MiB/64 MiB. Kết quả cuối đạt 14/14 recovery tập trung, 293/293 core và 9/9 suite R09; mọi FAIL ban đầu được giữ và sửa đúng nguyên nhân.

**Còn lại:** approval nghiệm thu đúng gói revision 13, không tự hoàn tất T05/R09. Coverage PARTIAL/NOT_RUN, WQ/vận hành hữu hạn, semantic impact refresh, monitoring/observer, T08 restore/release, G2 và nghiệm thu cuối vẫn mở theo phạm vi tương ứng. Task kỹ thuật tiếp theo là coverage reconciliation + WQ/impact refresh; độ khó cao, đề xuất **GPT-5.6 Sol + High**. R10 chưa mở; Android/iOS vẫn Future, Windows và Apple Silicon chưa được chứng minh bởi lượt này.

## Hiện hành — backend readiness/lifecycle component PASS

**Hiện hành, 2026-09-19.** Pilot source `4ee234e`, evidence commit local `a91e8fd` (Pilot không có remote). Backend nay tách `STARTING`, `READ_READY`, `WRITE_READY`, `DRAINING` và `STOPPED`. Đọc mở sau khi SQLite recovery/schema/epoch hợp lệ; ghi chỉ mở khi có receipt lab riêng tư cùng epoch, recovery point không quá 15 phút, đủ assertion backup/topology/receiver và probe ứng dụng có xác thực không quá 7 giây. Sample và probe trạng thái ghi không thể tự làm mới admission. Receipt sai quyền, sai epoch, lỗi định dạng, thiếu proof hoặc observer mất tín hiệu đều fail closed; drain chặn request mới trước khi chờ job đã nhận.

Manifest bất biến `3c9d85df…a27e338` khóa đúng nguồn cuối. Dev, ASan/UBSan, TSan và release đều exit 0; mỗi preset chạy 50 nhóm CTest/856 assertion, 14 lifecycle, 34 HTTP, 77 update, 174 telemetry, 20 backup, hai signal shutdown và tám session. Observer dev 35/35; Web trên 48 tệp khớp hash đạt sync, typecheck 0 diagnostics, lint, 102/102 unit và production build. TSan chỉ giữ đúng bảy ca/14 report SQLite-WAL kế thừa dưới `EX-T02-WAL-01-r2`; HTTP và lifecycle sạch. Lượt frozen đầu FAIL vì oracle ép trạng thái outbox phải còn pending dù dispatcher có thể hoàn tất hợp lệ; bản sửa chấp nhận đúng hai trạng thái có chứng cứ, vẫn giữ unit riêng bắt buộc đường pending và không nới sanitizer/safety gate.

Đây là **component PASS**, chưa phải readiness production hoặc nghiệm thu T05/R09. Receipt là cấu hình lab do operator xác nhận, không tự sinh proof; telemetry công khai vẫn `writeReady=false` với `BACKEND_WRITE_READINESS_NOT_EXPORTED`, M3/M7 UNKNOWN. Scheduler/retention, independent observer hiện thời, coverage còn thiếu, WQ, restore/release T08, impact ngữ nghĩa, G2 và Human acceptance vẫn mở. Public review revision 12 đã stale vì nguồn mới có ý nghĩa. Lượt tạo revision 13 bị helper từ chối `RECOVERY_REQUEST_TOO_LARGE` trước khi ghi; READ sau lỗi chỉ còn 20 chẩn đoán nguồn stale, Pilot sạch và không có pending-write diagnostic. Revision 13 chưa tồn tại, continuation chưa lưu. [Gói protocol v3 r1](../proposals/r09-recovery-protocol-v3-r1.md) giữ nguyên cap 128 MiB và chờ Human duyệt trước khi sửa helper/retry. Điểm tiếp theo: đối chiếu coverage và đo WQ/vận hành hữu hạn. Độ khó cao; đề xuất **GPT-5.6 Sol + High**. [Tóm tắt](../tests/evidence/r09/t05-readiness-lifecycle-r1/summary.json), [kiểm độc lập](../tests/evidence/r09/t05-readiness-lifecycle-r1/validation.json), [FAIL/PASS](../tests/evidence/r09/t05-readiness-lifecycle-r1/attempts.json), [nguồn Pilot](../tests/evidence/r09/t05-readiness-lifecycle-r1/pilot-source.json), [review bị chặn](../tests/evidence/r09/t05-readiness-lifecycle-r1/public-review-attempt.json).

## Hiện hành — backup online và bản sao lab độc lập đã được kiểm

**Hiện hành, 2026-09-19.** Pilot source được kiểm là `a1102ce`; evidence source ở `a8388e4`, metadata checkpoint ở `23e0588` (repo Pilot không remote). Thành phần backup dùng SQLite online backup, ghi watermark bền vững trong chính snapshot, xác minh hash/integrity/watermark và đo headroom thật. Snapshot cuối chuyển sang rollback-journal trước công bố, quyền `0600`, là một file portable không có WAL/SHM sidecar. Embedded writer/collector dùng gate nội bộ; collector chờ tối đa 100 ms cho request vừa hoàn tất rồi trả UNKNOWN nếu writer còn bận.

Manifest r5 `16e2bd76…b226d6` khóa đúng source `a1102ce`. Dev, ASan/UBSan, TSan và release đều exit 0; mỗi preset đạt 174 kiểm telemetry, 20 backup và 77 update, stderr sanitizer của ba phần đều rỗng. TSan giữ đúng 7 ca/14 report SQLite-WAL kế thừa thuộc EX r2; HTTP sạch, ngoại lệ/oracle không đổi. Các FAIL r1 writer-thread, r3 WAL sidecar, r4 HTTP overlap và authoring diagnostics được giữ.

Sau khi chốt metadata, bộ kiểm lõi Kidea trên Node `v24.21.0` đạt 293/293, không fail/cancel/skip/todo, stderr rỗng và xác nhận đầu vào không đổi.

Binary release tạo snapshot 2.408.448 byte, hash `2c4ebb1c…ad7e88`, `quick_check=ok`, journal `delete`, watermark/epoch đúng và không sidecar. Cùng file được chuyển bằng gcloud SCP tới Debian 12 x86_64 `e2-micro` độc lập trong project `kidea-508908`; verifier có sẵn trên VM xác nhận mode/owner/hash/immutable quick-check/watermark và fsync file+thư mục. Receipt ghi `INDEPENDENT_HOST_SNAPSHOT_VERIFIED`, chỉ đủ điều kiện recovery point của lab, không phải production. VM và boot disk đã xóa; hai inventory sau xóa đều rỗng. Bundle Git Pilot `23e0588` đã verify đủ lịch sử nhưng vẫn chỉ nằm cùng Mac, không thay bằng chứng host độc lập.

Public REVISE đã ghi `R09-T04-OUTPUT-r1` revision 12 **DRAFT**; SAVE trả `CONTINUATION_SAVED` và nói rõ đây không phải xác nhận task hoàn tất. `W-009-ADMIN-OPS` vẫn IN_PROGRESS, không có blocker; các impact ngữ nghĩa cũ còn cần làm mới. **T05/R09 chưa hoàn tất:** chưa có scheduler/retention/restore drill, admission/readiness/lifecycle, các nhánh chức năng còn thiếu, WQ, G2, output acceptance hoặc Human nghiệm thu. Điểm tiếp tục là admission/readiness/lifecycle rồi rà coverage/WQ; restore/release đầy đủ thuộc T08. Android/iOS Future, Windows/Apple Silicon chưa kiểm nguồn mới, R10 chưa mở. Task tiếp theo khó; đề xuất **GPT-5.6 Sol + High**. [Bằng chứng backup](../tests/evidence/r09/t05-backup-component-r1/summary.json), [FAIL/PASS](../tests/evidence/r09/t05-backup-component-r1/attempts.json), [public metadata](../tests/evidence/r09/t05-backup-component-r1/public-metadata.json), [cloud receipt](../tests/evidence/r09/t05-backup-component-r1/final/cloud/returned/receipt.json), [cleanup](../tests/evidence/r09/t05-backup-component-r1/final/cloud/post-delete-instances.json).

## Lịch sử — least-read collector bridge trên nguồn thật

**Hiện hành — cầu collector chỉ đọc đã được kiểm, 2026-09-19.** Pilot source cuối `8aca5d5`, checkpoint local `e26bafb` (không remote). Collector C++ mở kết nối SQLite riêng `READONLY` + `query_only`, dùng token riêng tư không thể thay bằng session ứng dụng, và xuất schema2 với boot/sequence/nonce cùng trạng thái UNKNOWN/PARTIAL theo từng tín hiệu. M1 là khoảng tuổi bảo thủ; M2 chỉ báo các lỗi thật đã biết và không biến producer chưa có thành số 0.

**Kết quả mới:** bốn preset đóng băng dev, ASan/UBSan, TSan và release hoàn tất. Dev/ASan/release đạt đủ 50 CTest; TSan giữ đúng 7 ca SQLite-WAL/14 report thuộc EX r2 đã duyệt, 43 ca còn lại sạch; 27 kiểm HTTP và controls sạch. Observer đạt 35/35. Binary release thật đi qua Caddy TLS riêng vào observer và Chrome đạt 7 kiểm: từ chối quyền/token giả, render schema2 UNKNOWN, không lộ credential, cảnh báo tuổi pending, boot đổi sau restart và giữ lịch sử nghiêm trọng, nguồn chết không xanh giả, thu hồi phiên xóa dữ liệu riêng. Ba lượt tích hợp FAIL trước đó được giữ và dẫn tới sửa Caddy capability, fixture pending và crash hiển thị UNKNOWN M8; không nới oracle.

Public REVISE tạo `R09-T04-OUTPUT-r1` revision 11 **DRAFT**; SAVE trả `CONTINUATION_SAVED`. `W-009-ADMIN-OPS` vẫn IN_PROGRESS, không blocker; impact cũ còn stale/NEEDS_CHANGE và G2/nghiệm thu chưa xảy ra. Bundle Git local Pilot đã verify, không phải backup độc lập máy. **T05/R09 còn IN_PROGRESS.**

**Điểm tiếp tục:** online backup có recovery-point watermark/receipt và số đo headroom đầy đủ; sau đó admission/readiness/lifecycle, các nhánh chức năng còn thiếu và WQ. M2 VERSION_CONFLICT/PERMISSION/reconciliation còn partial; M3/M7/M8/M9 UNKNOWN, dashboard probe là fixture. Chưa có độc lập host/E1/Human oncall/PROD/T08/G2; Android/iOS Future, Windows/Apple Silicon chưa kiểm nguồn mới, R10 chưa mở. Task tiếp theo khó; đề xuất **GPT-5.6 Sol + High**. [Bằng chứng collector](../tests/evidence/r09/t05-collector-bridge-r1/final/inventory.json), [Chrome](../tests/evidence/r09/t05-collector-bridge-r1/final/browser-result.json), [ba FAIL và PASS cuối](../tests/evidence/r09/t05-collector-bridge-r1/final/attempts.json), [bundle Pilot](../tests/evidence/r09/t05-collector-bridge-r1/pilot-local-bundle.json).

## Lịch sử — partial real telemetry và primary W5

[Proof nguồn/kết quả](../tests/evidence/r09/t05-real-telemetry-r1/final-checks.json),[bốn preset](../tests/evidence/r09/t05-real-telemetry-r1/backend-build-r2/final-backend-checks.json),[TLS144/SQL27](../tests/evidence/r09/t05-real-telemetry-r1/integration-r3/collection.json),[facets/còn thiếu](../tests/evidence/r09/t05-real-telemetry-r1/facets.md),[preservation](../tests/evidence/r09/t05-real-telemetry-r1/finalization-r3/preservation.json). Integration-r1 FAIL tại429 sau22ca cũ,ops NOT_RUN;r2 Chrome144PASS nhưng lượt tổng thể FAIL ở bộ audit SQL cũ còn giả định observation là nhóm cuối;ops SQL NOT_RUN. R3 chia ranh giới bằng audit thật và kiểm hết phần ops sau đó,144browser/27SQL PASS là kết quả cuối. Lỗi Web thật:mất known-critical khi catalogUNKNOWN/chấp nhận epoch cũ trong lastSuccess. Hai preimageFAIL giữ riêng;nguồn cuối siết guards/chạy đầy đủ,không sửa oracle. Authoring npmcache/type/lint/caller/build tidy giữ raw riêng.

Bài học Kidea:phân biệt lỗi app/caller/helper;bind danh tính cả envelope/dữ liệu lồng;UNKNOWN không xóa lỗi đã biết;batch không là tổng pending;mốc trướcCOMMIT không là commit chính xác;PASS thành phần không nâng gate toàn dự án. Compiler scratch chỉ do lượt này tạo đã nén/kiểm toàn bộ byte/mode/hash rồi giữ binaries cần thiết;receipts/local archive thuộc proof,không chạm build/data trước phiên. Private CA keys không publish. Cùng baseline20GiB/free100GiB/Gitreserve128MiB;không cài/pull/cloud/deploy/hosttrust/ports mới.

## Lịch sử — khép phục hồi checkpoint observer, 2026-09-19

Human “tôi duyệt, làm tiếp đi” đã cấp quyền mới cho đúng [gói recovery r1](../proposals/r09-observer-checkpoint-recovery-r1.md), patch SHA256 `41347c4152def2f194e5a1e26b22f8cd924bfe4bf51f446826d26b30beb8a11c`. [Authorization](../tests/evidence/r09/t05-observer-recovery-r1/authorization.json) ghi đúng operation/digest/target và phạm vi; không phải nghiệm thu R09. Đã áp đúng bốn file đóng băng, gồm hai module, contract và test mới. [Applied hashes](../tests/evidence/r09/t05-observer-recovery-r1/applied-patch.json).

Trên [Mac Intel/tài khoản thường/Node 24 có sẵn](../tests/evidence/r09/t05-observer-recovery-r1/environment.json), dependency theo lockfile qua offline npm ci. **293/293 core và 9/9 recovery PASS**, 0 skip/cancel/todo; [core raw](../tests/evidence/r09/t05-observer-recovery-r1/core-raw/summary.json), [recovery raw](../tests/evidence/r09/t05-observer-recovery-r1/recovery-tests.stdout.log). Đây là lượt mới trên bản Kidea đang dùng sau áp patch; core before/after không đổi, các hash cuối khớp patch duyệt. Đã đưa năm ca mới vào nhóm recovery của [runner R09 thường quy](../tests/r09/run-tests.mjs), giữ mọi suite cũ. [Lượt full R09 r1](../tests/evidence/r09/t05-observer-recovery-r1/full-r09-r1-raw/summary.json) tổng thể FAIL: 7 nhóm PASS, R06 và R06-boundaries FAIL vì caller thiếu env compiler/parser/sample. [Live tools](../tests/evidence/r09/t05-observer-recovery-r1/r06-live-tools.json) xác minh lại Clang/macOS SDK/parser/mẫu có sẵn; chỉ thiết lập env cho process kiểm, không cài/thay cấu hình máy. [Hai nhóm R06 chạy lại đầy đủ](../tests/evidence/r09/t05-observer-recovery-r1/r06-recheck-results.json) PASS; source và kỳ vọng không đổi. Cả chín nhóm có kết quả cuối PASS trên cùng source/hash: bảy nhóm từ full-r1, hai nhóm từ recheck-r2. Không gọi full-r1 là PASS hoặc bỏ các ca từng lỗi. Trần prepared-request 128 MiB thống nhất writer/reader; mỗi source/copy/checkpoint vẫn 64 MiB. Test mới kiểm vượt trần trước tạo guard, grant/hash/operation/digest sai, stale basis/source/OTHER/runtime khác vẫn bị chặn. Các FAIL gốc và candidate được giữ ở các hồ sơ phía dưới; không thay kỳ vọng/giảm kiểm để lấy PASS. Host Node 22 mặc định không đổi; Windows và Apple Silicon chưa kiểm bản vá.

[Public READ_RECOVERY mới](../tests/evidence/r09/t05-observer-recovery-r1/read-recovery-result.json) dùng statement từ quyết định Human thật, không dùng basis/grant của candidate inspection. Xác nhận đúng project/owner/checkout và sole target BEFORE. [Public RECOVER](../tests/evidence/r09/t05-observer-recovery-r1/recover-result.json) trả **RECOVERY_COMPLETED**. [Preservation](../tests/evidence/r09/t05-observer-recovery-r1/preservation.json) kiểm exact planned bytes, chỉ nextAction/checkpointRef đổi; mọi dữ liệu work khác, toàn bộ reviews/evidence, source observer và harness giữ nguyên. Prepared-request 83.111.093 byte/digest/tool identity gốc và copies giữ nguyên; journal giữ quyết định mới và checkpoint preimage, checkpoint bổ sung observation readback. Helper gỡ đúng marker sau đầy đủ kiểm; không restore thủ công hoặc replay external effects. Chỉ là metadata reconciliation, không là product completion/VERIFIED live health.

[Pilot checkpoint](../tests/evidence/r09/t05-observer-recovery-r1/pilot-checkpoint.json) đã commit local `aba73cf246f2ccfd8a30d0322048dc0a5934bebb`, source observer vẫn `8779453`, clean/không remote. Review revision 9 DRAFT, `W-009-ADMIN-OPS` IN_PROGRESS; mọi gates/history/T04 acceptance giữ nguyên. Điểm tiếp tục nay đã nằm trong work/checkpoint thực. [Public resume READ từ commit mới](../tests/evidence/r09/t05-observer-recovery-r1/resume-final-result.json) trả WAITING, đúng owner và checkout; graph đọc được, các blockers/gate nghiệp vụ vẫn giữ. Thời gian 596,33 s là timing thực của hồ sơ này, không phải WQ/performance PASS. Không retry SAVE/REVISE cũ hoặc tạo một checkpoint ứng dụng giả.

**Phần tiếp theo:** nối nguồn pending/tuổi/lỗi thật C++ và primary W5 theo [bridge assessment](../tests/evidence/r09/t05-observer-authoring-r1/real-source-bridge-assessment.json). Hiện outbox chưa có durable commit timestamp, pending() chỉ đọc batch 64, chưa có durable open-error catalog; không suy tuổi từ ID hoặc coi batch là tổng pending. Backend thay đổi phải chạy mới đầy đủ bốn preset dưới EX r2 và trần tài nguyên cùng baseline. Observer 29 Mac/15 Docker Chrome PASS chỉ là bằng chứng thành phần dùng lại: số đo/quyền/backup đang giả lập; `writeReady=false` vì backend admission chưa nối. Backend/Web 132 HTTPS/22 SQL/bốn preset cũ dùng lại, không được báo là kiểm mới của lát recovery.

Coverage 137 giữ 11 covered/88 partial/38 NOT_RUN; không nâng full OP. Backup/watermark/receipt/admission/readiness/lifecycle/WQ, các nhánh chức năng/input/arrival còn thiếu, host độc lập/E1/Human PROD/oncall/T08release-restore/semantic impacts/G2/nghiệm thu vẫn mở. Native Future chưa roadmap, Apple Silicon NOT_RUN, R10 chưa mở. Task kế tiếp có độ khó cao; đề xuất **GPT-5.6 Sol + High** để xử lý C++/SQLite, phân quyền và đối chiếu số đo; [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol). Đánh giá lựa chọn cho task này là nhận định của agent, không phải khuyến nghị riêng từ OpenAI cho Kidea.

## Lịch sử trước duyệt — observer thành phần PASS; checkpoint chờ phục hồi, 2026-09-18–19

Observer và màn dự phòng nguồn pilot `87794537998da2b1603088216c7d3344e9925b1e` đã qua **29/29 kiểm Mac unit/component, offline npm ci/lint và 15/15 nhóm Docker Linux Chrome/TLS PASS**,0 skip/cancel/todo. [Nguồn cuối](../tests/evidence/r09/t05-observer-component-r5/tested-source/observer/README.md), [checks](../tests/evidence/r09/t05-observer-authoring-r1/final-checks-r1/checks.json), [browser](../tests/evidence/r09/t05-observer-component-r5/run/browser/result.json). TLS, fsync, dừng/khởi động tiến trình con thuộc lượt kiểm và việc hiển thị tại Chrome là thật; toàn bộ M1–M9, quyền, oncall, backup source là fixture giả lập. M1 receiver đo 1569 ms từ stimulus/1269 ms từ phát hiện; nguồn Node giả lập dừng được hiển thị sau 9800 ms/1848 ms, giữ mục tiêu 8/17 s và detect-to-show 5 s. Cùng kernel/clock không chứng minh E1, host hỏng, AI-exit, Human oncall, tuổi pending C++ hoặc backup thật. Đây là fallback riêng, chưa là primary SvelteKit W5. `writeReady` luôn false/`BACKEND_ADMISSION_NOT_WIRED`, chưa thành admission gate tại backend.

[Facet OP-T01–15](../tests/evidence/r09/t05-observer-authoring-r1/component-facets.md) giữ phần đã kiểm và còn thiếu. Trong phạm vi thành phần đã kiểm private 0700/0600/UID guards; schema/nonce/sequence/clock/freshness; known critical trong UNKNOWN; fsync state; phục hồi theo nhịp nguồn; quyền đọc/xóa DOM/fence phản hồi muộn. [Năm lượt](../tests/evidence/r09/t05-observer-authoring-r1/attempts-and-source-reconstruction.json) giữ 273 raw files và nguồn mỗi manifest khớp hash. r1/r2 FAIL thuộc executable/preflight/bind ownership; r3 assertions đạt nhưng recorder đè hai ID; r4 PASS rồi thêm comment lint; r5 chạy lại đủ 15 trên nguồn cuối và PASS. Tài khoản thường tạo child private đúng UID; không giảm guard/oracle. [Authoring diagnostics](../tests/evidence/r09/t05-observer-authoring-r1/authoring-diagnostics.json) giữ các lỗi wrapper/lint/cwd. [Ảnh360px/chữ200%](../tests/evidence/r09/t05-observer-component-r5/run/browser/critical-360.png) đã xem, không tràn ngang.

[Source audit](../tests/evidence/r09/t05-observer-authoring-r1/final-source-audit.json) giữ 112 nguồn backend và toàn bộ Web source/build/dependencies. 132 HTTPS/22 SQL/core 293/bốn preset/EXr2 **dùng lại**, không chạy mới trong lát observer. Ledger/expected 137 gốc giữ 11 covered/88 partial/38 NOT_RUN, không nâng full OP. Không cài/pull/cloud/deploy/thay host trust hoặc mở host ports. [Cleanup](../tests/evidence/r09/t05-observer-authoring-r1/cleanup-final.json) gỡ đúng tài nguyên năm lượt, không prune lịch sử; vẫn cùng baseline 20 GiB/free 100 GiB/Git reserve 128 MiB.

**Lỗi lưu hồ sơ hiện tại:** public REVISE trả REVIEW_RECORDED revision 9 DRAFT (899,57s), READ WAITING (554,99s), giữ owner/reviews/history. SAVE bị bộ điều phối tác giả đặt 1200 s quá ngắn ngắt; [raw timeout](../tests/evidence/r09/t05-observer-authoring-r1/finalization-r2/finalization-timeout.stderr.log), [diagnostic](../tests/evidence/r09/t05-observer-authoring-r1/finalization-r2/timeout-diagnostic.json). Marker `bef9054c-51f3-4f07-a54b-0892fc95247d` còn active. Work BEFORE không đổi, pilot HEAD 8779453 giữ nguyên; chưa commit checkpoint/package metadata. Reader Kidea đang dùng trả [RECOVERY_FILE_UNSUPPORTED](../tests/evidence/r09/t05-observer-authoring-r1/finalization-r2/recovery-r1/read-recovery-result.json) vì request 83.111.093 byte vượt cap 64 MiB. Đây là lỗi khớp writer/reader của Kidea, tách khỏi observer test PASS. Không retry blind, gỡ marker hoặc restore thủ công.

**[Gói duyệt phục hồi r1](../proposals/r09-observer-checkpoint-recovery-r1.md)** đã có patch riêng: prepared-request 128 MiB ở hai bên, source/copy/checkpoint 64 MiB, preflight từ chối trước guard. Chỉ cho phép migration rõ ràng của đúng hai module/hash và đúng operation/digest; không thay Node/runtime hoặc module khác. [Candidate manifest/patch/core293/recovery9](../tests/evidence/r09/t05-observer-authoring-r1/recovery-proposal-r1/manifest.json) giữ các FAIL wrapper/sourceRefs/contextRefs. 13 kiểm cycle/delivery/interop PASS trong mixed-r4 toàn bộ 21 PASS/1 FAIL do fixture mới thiếu context refs; đã thêm snapshot/ref thực của fixture và chạy kiểm cuối recovery 9/9 PASS trên helper cuối. Core 293 candidate chạy đầy đủ, không skip/cancel/todo; đây không là PASS mới của bản đang dùng. [Candidate public READ_RECOVERY](../tests/evidence/r09/t05-observer-authoring-r1/finalization-r2/candidate-recovery-r1/candidate-read-result.json) chỉ đọc trả RECOVERY_READY/target BEFORE, source/Git/hash/projected graph khớp, work/marker không đổi. Candidate chưa áp vào Kidea, chưa RECOVER/chưa CONTINUATION_SAVED; không thành VERIFIED progress/G2/Human acceptance. [Kiểm kê bytes/diff](../tests/evidence/r09/t05-observer-authoring-r1/recovery-proposal-r1/exact-scope.json) xác nhận chỉ UPDATE `.kidea/work.md`: nextAction/checkpointRef, mọi work data khác giữ nguyên. Node 24 có sẵn, host 22 mặc định không đổi; không khóa patch chung. Windows và Apple Silicon chưa kiểm bản vá.

**Điểm tiếp tục:** Human duyệt một gói cap/migration/exact completion; áp đúng patch, kiểm trên nguồn thực cuối, tạo READ_RECOVERY/basis/grant mới từ quyết định thật rồi public RECOVER, commit pilot local và commit/push Kidea. Không dùng statement/basis của candidate inspection làm approval. Sau đó nối [nguồn C++ thật và primary W5](../tests/evidence/r09/t05-observer-authoring-r1/real-source-bridge-assessment.json): outbox chưa có durable commit timestamp, pending() chỉ đọc batch 64, chưa có durable error catalog. Không suy tuổi từ ID/UI hoặc lấy batch làm tổng pending. Khi đổi C++ phải kiểm mới đủ bốn preset với EXr2 trên nguồn cuối.

Backup/watermark/receipt/admission/readiness/lifecycle/WQ, các nghĩa vụ chức năng/input/arrival còn thiếu, host độc lập/E1/Human PROD/oncall/T08release-restore/semantic impacts/G2/Human nghiệm thu vẫn mở. T05/W-009-ADMIN-OPS IN_PROGRESS, review9 DRAFT; native Future chưa roadmap, R10 chưa mở. Khép phục hồi có độ khó cao, đề xuất **GPT-5.6 Sol + High**; [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol).

## Lịch sử — độ mới của quan sát và quyền truy cập, 2026-09-18

Đã hoàn thành lát kỹ thuật này theo quyền trọn R09. T05/`W-009-ADMIN-OPS` vẫn IN_PROGRESS; chưa G2 hoặc nghiệm thu R09.

- [Tích hợp cuối](../tests/evidence/r09/t05-admin-observation-integration-r3/run/browser/result.json): **132/132 HTTPS/WSS PASS**, giữ đủ 100 ca trước và thêm 32 nhóm: 6 thay đổi chỉ số đăng ký N qua WSS thật trong cửa sổ capacity/schedule/state, 9 nhánh lỗi đọc/phiên được tiêm có kiểm soát, 15 ô quyền theo actor × DRAFT/OPEN/PAUSED, giả quyền và thu hồi phiên thật. Xác nhận cũ đóng, không POST cho đến khi xem/xác nhận lại; giữ trường đã nhập. Matrix áp dụng các route hiện đã triển khai, không bao gồm monitoring hoặc cache/deployment chưa kiểm.
- **Hai lỗi pilot Web đã sửa**, nguồn `45d3808a3fa776089f985918fd65b0a1cec3d585`: bản đọc cũ được trình bày như dữ liệu mới; thiếu kiểm lại phiên sau khi đọc snapshot để xác nhận. [Baseline cuối 3 FAIL/5 PASS](../tests/evidence/r09/t05-admin-observation-authoring-r1/native-baseline-r2.stdout.log) giữ nguyên. Baseline đầu có điều kiện chờ quá sớm; không dựa vào các PASS của nó. Hai timeout của lượt Web-r1 do chưa nhận diện thông báo mất quyền cũng giữ riêng; sửa phần chờ, không giảm kiểm zero-POST/ẩn dữ liệu. [Web-r2](../tests/evidence/r09/t05-admin-observation-authoring-r1/web-r2/checks.json) chạy đầy đủ: 93 unit, 18 native SSR, 4 content + 4 sensitive + 8 observation regressions, typecheck 0 warning/lint/build PASS, không skip. Native regression dùng fake API/socket; tách rõ khỏi backend/TLS thật.
- [SQL 22 checks](../tests/evidence/r09/t05-admin-observation-integration-r3/run/browser/matrix-audit.json) giữ đủ 18 checks cũ; mới đối chiếu 48 admin intents hợp lệ (42 APPLIED/6 NO_CHANGE), 30 participant requests và 18 workshops. 36 admin packets bị từ chối có đúng 36 DENIED audit với ID server riêng (18 UNAUTHENTICATED/9 PERM-OWNER/9 PERM-OTHER), ràng buộc bằng row đầu của phase sở hữu, không tin client ID hoặc đồng hồ; 21 personal packets bị từ chối không có result hoặc domain/outbox mutation. Kiểm actor/payload/result/audit/delta/version/outbox, 52 raw sockets và lịch sử ACTIVE/CANCELLED riêng. Đây là số fixture được theo dõi, không là tổng database. [Wire proof](../tests/evidence/r09/t05-admin-observation-integration-r3/run/browser/requests.json) kiểm cả request sau bỏ route/reload. DRAFT qua public WSS chỉ nhận đúng một SUBSCRIBED (epoch/audience) rồi đóng, không snapshot hoặc dữ liệu domain; lỗi quyền/CSRF/epoch/Origin vẫn không frame. Lượt integration-r2 browser132 PASS nhưng SQL FAIL do auditor tìm ID audit từ client; giữ chẩn đoán và sửa đối chiếu theo ID server với số lượng/actor/empty delta/no mutation nguyên vẹn. Lượt integration-r1 FAIL do oracle mới nhầm ACK với domain, giữ nguyên và chẩn đoán theo hợp đồng protocol; private SUBSCRIBE phải bỏ workshopId. HTTP công khai và WSS invalid-cookie có hợp đồng khác nhau; SSR `/me` trả 401 cho admin-only, API `/me` trả 404. Giữ MVP PAUSED chặn cả register/cancel mới; T09 chưa triển khai.
- [Coverage 137](../tests/evidence/r09/t05-admin-observation-authoring-r1/coverage-audit-r1/coverage.md): **11 COVERED_FUNCTIONAL/88 PARTIAL/38 NOT_RUN**, chỉ P01 được nâng thêm trong lát này, có cả nhãn PAUSED từ kiểm SSR giữ lại. P02 còn monitoring nên vẫn PARTIAL. Ledger/expected/sourcehash gốc và Max2 riêng giữ nguyên. [16 negative proof fixtures](../tests/evidence/r09/t05-admin-observation-authoring-r1/audit-final-tests.json) gồm đủ 9 fixture cũ đều bị từ chối trước output; relative/absolute JSON byte-identical. Đây không phải 16 pilot runs hoặc 137 task T05.
- [Source audit](../tests/evidence/r09/t05-admin-observation-authoring-r1/source-audit-final.json) kiểm 112 backend manifest files (56 execution/56 docs); artifact/bốn preset/EX r2 và core 293 dùng lại, không build/core mới. Kidea runtime/package không đổi. [Bảo toàn lịch sử](../tests/evidence/r09/t05-admin-observation-authoring-r1/historical-evidence-preservation.json) kiểm 2.084 raw files/32 collection cùng ledger gốc; Windows evidence không đổi, không mở SQLite lưu trữ. Không suy physical I/O/crash/restore/load từ fault đọc được tiêm.

Public REVISE ghi REVIEW_RECORDED, revision8 DRAFT; READ cuối WAITING; [SAVE](../tests/evidence/r09/t05-admin-observation-authoring-r1/finalization-r1/resume-save-final-result.json) CONTINUATION_SAVED. [Preservation](../tests/evidence/r09/t05-admin-observation-authoring-r1/finalization-r1/preservation.json) giữ owners/gates/workstate/reviews/history; chỉ nextAction/checkpointRef thay đổi. Pilot local commit `c8b73877c22459f171f1a2a6acc3a62c33d7b856`, sạch/không remote; [receipt](../tests/evidence/r09/t05-admin-observation-authoring-r1/finalization-r1/pilot-checkpoint-final.json). [Cleanup](../tests/evidence/r09/t05-admin-observation-authoring-r1/cleanup-final.json) không còn container/network của lượt này, không prune lịch sử. Đĩa dùng cùng baseline: 14.83 GiB/20 GiB mới, 323.6 GiB trống, dự phòng Git 128 MiB. Không cài/download/config host/cloud/deploy.

Docker client wait tăng 360→600 giây để chứa 32 nhóm mới với pacing 1100ms giữ nguyên; deadline từng request 30s/container 20 phút/run 30 phút và trần tài nguyên cũ giữ nguyên. Đây là thời gian harness chức năng, không đổi mục tiêu performance hoặc an toàn.

**Điểm tiếp tục:** các nghĩa vụ lifecycle/input/render/arrival-point và lỗi còn thiếu được ghi từng dòng trong coverage; tiếp theo triển khai monitoring/observer và readiness, rồi online backup/watermark/WQ. Release/restore ở T08, semantic impacts/G2/Human nghiệm thu vẫn mở. Android/iOS Future chưa roadmap; Apple Silicon NOT_RUN; R10 chưa mở. Task monitoring/observer/readiness có độ khó cao, đề xuất **GPT-5.6 Sol + High** theo phạm vi công việc; [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol).

## Lịch sử — xác nhận thay đổi, hai form nhạy cảm và PAUSE, 2026-09-18

Đã hoàn thành lát kỹ thuật theo quyền trọn R09; T05/`W-009-ADMIN-OPS` vẫn IN_PROGRESS, chưa G2/nghiệm thu R09.

- [Tích hợp cuối](../tests/evidence/r09/t05-admin-sensitive-integration-r1/run/browser/result.json) **100/100 HTTPS/WSS PASS**, giữ đủ85 kiểm cũ và thêm15 nhóm:7biến thể hai form capacity/schedule/state,4thay đổi nguồn xác nhận qua WSS thật,4thứ tự PAUSE với register/cancel. Hai POST được giữ sau xác nhận/trước backend quyết định rồi thả theo từng thứ tự; patch chỉ gồm trường đã sửa, không áp đặt khóa/ưu tiên admin. Hai PAUSE giống nhau: lần sau NO_CHANGE, không thêm event.
- **Một lỗi pilot Web thật đã sửa**, nguồn `b496e88c182ebcf817017829244d5d532569ec97`: cửa sổ STATE còn mở sau thay đổi nguồn đã biết khi không có trường dirty. Bản sửa hủy xác nhận cũ, yêu cầu xem/xác nhận lại trước POST; giữ dữ liệu người dùng đã nhập. [Baseline2FAIL/2PASS](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/native-baseline.stdout.log) và ảnh/DOM giữ nguyên; đây là fake API/socket regression, khác integration backend thật. [Web mới](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/web-r1/checks.json):93unit/18nativeSSR–ChromeMac/4regression cũ+4mới/typecheck0warning/lint/build PASS, khôngskip.
- [SQL18checks](../tests/evidence/r09/t05-admin-sensitive-integration-r1/run/browser/matrix-audit.json) PASS:66+18+56tracked admin intents,20+9+15workshops;17participant request records mới. Kiểm đúng actor/payload/result/audit/delta/version/outbox và registrationID; no-op/PAUSED rejection không event. Quy tắc MVP hiện hành PAUSED chặn cả register và cancel mới; cancellation trong PAUSED là thay đổi nghiệp vụ T09, chưa triển khai. [Wire proof](../tests/evidence/r09/t05-admin-sensitive-integration-r1/run/browser/requests.json) kiểm cả reload/sau bỏ route, không gửi lặp.
- [Coverage137](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/coverage-audit-r1/coverage.md):10COVERED_FUNCTIONAL/89PARTIAL/38NOT_RUN; chỉ AD-T05 được nâng ở lát này. AD-T06/AD-T13/AR-T07 còn thiếu biến thể; ledger/expected/sourcehash gốc giữ nguyên, Max2 riêng. [9negative proof fixtures](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/audit-final-tests.json) đều bị từ chối trước output; relative/absolute JSON byte-identical. Không phải9pilotrun hoặc137taskT05.
- [Đối chiếu nguồn](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/source-audit-final.json):112backendmanifestfiles gồm56execution+56docs đúng hash; artifact/bốn preset/EXr2 và core293 dùng lại, không lượt backend/core mới. Kidea runtime/package không đổi. [Bảo toàn](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/final-closure.json) giữ đủ85selectors/ledger và toàn bộ collection cũ nguyên byte. Không chứng minh physicalI/O/crash/backup/tải từ các kiểm chức năng này.

Public REVISE trả REVIEW_RECORDED, revision7 DRAFT; READ cuối WAITING và [SAVE](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/finalization-r1/resume-save-final-result.json) CONTINUATION_SAVED. [Preservation](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/finalization-r1/preservation.json) giữ mọi owner/gate/workstate/review/history, chỉ nextAction/checkpointRef đổi. Pilot local commit `cc604c4e8f6b0f8c2e24214932c69ad26e499960`, sạch/không remote; [receipt](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/finalization-r1/pilot-checkpoint-final.json). [Cleanup](../tests/evidence/r09/t05-admin-sensitive-authoring-r1/cleanup-final.json) không còn container/mạng của lượt này, không prune lịch sử. Đĩa cùng baseline: 14.57GiB/20GiB mới, 323.9GiB trống, dự phòng Git128MiB. Không install/download/config host/cloud/deploy, Windows evidence không đổi.

Bộ kiểm chờ Docker tăng180→360giây để chứa15nhóm mới với pacing1100ms giữ nguyên; deadline từng request30s/container20phút/run30phút và trần tài nguyên cũ còn nguyên. Đây là thời gian harness chức năng, không đổi mục tiêu performance/an toàn.

**Còn lại:** thay đổi chỉ N trong lúc xác nhận và các nhánh observation lỗi/chưa xác nhận; ma trận quyền G/U/admin-only/forged/revoked trên HTTP/SSR/socket đầy đủ; rồi monitoring/independent observer, online backup/watermark/readiness/WQ. Release/restore ởT08, semantic impacts/G2/Human nghiệm thu còn mở. NativeFuture chưa roadmap, AppleSiliconNOT_RUN, R10 chưa mở. Task tiếp: khép các biên observation/quyền còn thiếu, độ khó cao, đề xuất **GPT-5.6 Sol + High** theo đánh giá phạm vi, không cam kết chi phí tài khoản; [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol).

## Lịch sử — publication failure, actor/namespace và hai form, 2026-09-18

Đã hoàn thành lát kỹ thuật này theo quyền trọn R09. T05/`W-009-ADMIN-OPS` vẫn IN_PROGRESS; chưa G2/nghiệm thu R09.

- [Tích hợp cuối r5](../tests/evidence/r09/t05-admin-boundaries-integration-r5/run/browser/result.json) **85/85 HTTPS/WSS PASS**, giữ đủ77 kiểm cũ và thêm8 nhóm. [Runner](../tests/evidence/r09/t05-admin-boundaries-integration-r5/run/result.json) PASS theo manifest3fee131b; source/plan/build/dependencies không đổi trước–sau. Caddy TLS/backend thật/Docker Chromium, không bypass TLS.
- Publication STATE abort thật trong SQLite audit transaction trả503, hai reload/GET202UNKNOWN giữ khóa, không POST lặp; EDIT đã commit trước vẫn tồn tại và có xác nhận lịch sử riêng đã revalidate cùng actor/epoch/workshop/intent. Trigger giả chỉ ở DB sở hữu, không chứng minh physical disk/I/O/crash/backup. Absent STATE cũng không được tự gửi lại.
- Exact packet replay giữ một kết quả; changed payload409 và other-admin404; hai participant giả riêng BOUNDARY-U/V và adminB dùng cùng literalID vẫn tách namespace/owner/result. Actor A→B→A không lộ intent/lịch sử A cho B; A phục hồi qua GET. Hai form khác trường giữ cả hai patch, cùng trường theo cả hai thứ tự commit. [SQL14checks](../tests/evidence/r09/t05-admin-boundaries-integration-r5/run/browser/matrix-audit.json) PASS:66+18tracked intent/20+9workshop; không là tổng toànDB. [Wire proof](../tests/evidence/r09/t05-admin-boundaries-integration-r5/run/browser/requests.json) kiểm cả reload/sau bỏ route.
- **Hai lỗi pilot Web thật đã sửa**, nguồn4544bd6: thiếu xác nhận EDIT lịch sử riêng sau publication failure; dữ liệu mới ghi đè cảnh báo UNKNOWN. [Baseline FAIL và các lần thử](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/findings-and-attempts.json) giữ nguyên. [Web mới](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/web-r1/checks.json):93unit/18nativeSSR–ChromeMac/4regression/typecheck0warn/lint/build PASS, khôngskip. Kidea runtime/package không đổi; backendartifact/bốn preset/EXr2 và core293 dùng lại, không build/core mới. [Source audit](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/source-audit-final.json) kiểm112manifestfiles gồm56executionfiles và56docs, tất cả không đổi.
- R1 lỗi Web thật; r2 Chromium body503 observer failure (nguyên nhân cụ thể chưa xác định); r3 tái dùng tài khoản hết quota, backend trả LIMIT_REACHED đúng; r4 payload khác nhắm DRAFT riêng tư nên visibility404 trướcconflict409. Sửa nguồn Web và điều kiện/quan sát bộ kiểm, không giảm an toàn/Max2/quyền hoặc sửa kỳ vọng để PASS. [Chẩn đoán](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/stop-diagnostic.md); rawFAIL/frozenplan từng lượt giữ riêng.
- [Coverage137](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/coverage-audit-r1/coverage.md):9COVERED_FUNCTIONAL/90PARTIAL/38NOT_RUN; ledger/expected/sourcehash gốc nguyên byte, Max2 riêng. [6negative proof fixtures](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/audit-final-tests.json) đều bị từ chối đúng trướcoutput; paths relative/absolute cho JSON byte-identical. Đây không6pilotrun hoặc137taskT05.

Public REVISE đã trả REVIEW_RECORDED, revision6 DRAFT; READ cuối WAITING và [SAVE](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/finalization-r1/resume-save-final-result.json) CONTINUATION_SAVED. [Bảo toàn](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/finalization-r1/preservation.json) PASS: chỉ nextAction/checkpointRef thay đổi, các workstate/gate/owner/review khác giữ nguyên. Pilot local commit `eb0aef6213d7ea349d07c9349b9753b7865d014e`, sạch, không remote; [receipt](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/finalization-r1/pilot-checkpoint-final.json). [Cleanup](../tests/evidence/r09/t05-admin-boundaries-authoring-r1/cleanup-final.json) xác minh cả5lượt không còn container/mạng sở hữu. Không install/download/config host/cloud/deploy; Windows evidence không đổi.

**Còn lại:** sensitive two-form capacity/schedule/state, PAUSE/register-cancel arrival orders và ma trận quyền đầy đủ; monitoring/independent observer, online backup/watermark/readiness/WQ. Release/restore đầy đủ ởT08, semantic impacts/G2 và Human nghiệm thu vẫn mở. Native Future chưa roadmap; AppleSilicon NOT_RUN; R10 chưa mở. Task tiếp: phần concurrency/PAUSE còn thiếu, độ khó cao, đề xuất **GPT-5.6 Sol + High** theo đánh giá phạm vi; không cam kết chi phí tài khoản.

## Lịch sử — mất phản hồi, nhấp đúp và hai thứ tự tranh chấp, ngày 2026-09-18

Đã hoàn thành phần kiểm kỹ thuật đã chọn theo quyền trọn R09. T05/`W-009-ADMIN-OPS` vẫn IN_PROGRESS; chưa G2 hoặc nghiệm thu R09. Không nhập mọi nghĩa vụ của toàn pilot thành số task T05.

- [Tích hợp cuối r3](../tests/evidence/r09/t05-admin-faults-integration-r3/run/browser/result.json): **77/77 HTTPS/WSS PASS**, giữ đủ71 kiểm cũ và thêm6 nhóm. Backend/Caddy TLS/Chromium Docker thật, không bypass TLS. [Runner](../tests/evidence/r09/t05-admin-faults-integration-r3/run/result.json) PASS trên [manifest đóng băng](../tests/evidence/r09/t05-admin-faults-integration-r3/manifest.json); source/plan/build/dependency được kiểm trước–sau và không thay sau kiểm. [Đối chiếu giữ kiểm cũ](../tests/evidence/r09/t05-admin-faults-authoring-r1/retained-checks.json).
- C10,N9→C9 được kiểm ở cả hai thứ tự: registration thắng→BELOW_ACTIVE/C10,N10; capacity thắng→UPDATED rồi đăng ký thứ10 FULL/C9,N9, không tăng version/event vì bị từ chối. CREATE/EDIT chưa tới backend: hai lần reload/tra cùng intent đều trả202/stateUNKNOWN, giữ khóa thao tác, không POST thay thế; CREATE không tìm theo tên/tự chọn ID. Nhấp đúp pointer CREATE/EDIT/STATE đều chỉ một POST, một thay đổi version và durable result/audit. Chỉ drop/hold/thứ tự được điều khiển, kết quả nghiệp vụ và ghi dữ liệu là thật.
- [SQL audit sau dừng/dọn](../tests/evidence/r09/t05-admin-faults-integration-r3/run/browser/matrix-audit.json) PASS: **66 admin intents/20 workshop thay đổi**, exact delta/version/outbox, không event cho noop/reject/GET; marker token/CSRF/nội dung riêng giả không có trong audit. Ý định chưa gửi tới backend không có result/audit/workshop mới. Đây là audit sau shutdown, không crash/restore/backup/performance.
- [Đối chiếu nguồn/artifact](../tests/evidence/r09/t05-admin-faults-authoring-r1/source-audit.json): backend112nguồn/artifact1fb4a69 và Web2607aa6 source/build/dependencies không đổi. Bốn preset/EXr2, Web93unit/18native SSR–Chrome Mac và core293 là bằng chứng cũ dùng lại; không gọi là lượt build/unit/core mới. [Closure cuối](../tests/evidence/r09/t05-admin-faults-authoring-r1/final-closure.json) xác nhận Kidea runtime/package cũng không đổi.
- [Audit137 hiện hành](../tests/evidence/r09/t05-admin-faults-authoring-r1/coverage-audit-relative-r3/coverage.md):5COVERED_FUNCTIONAL/94PARTIAL/38NOT_RUN; thêm chứng cứ đủ chức năng cho đúng AD-T04/AR-T06, còn AD-T08 partial (absent STATE và các biến thể còn thiếu được nêu rõ). Đây là đánh giá137 nghĩa vụ toàn pilot, không137 kiểm thất bại, không137 task T05. Ledger/expected/source hash gốc nguyên byte;4ca Max2 giữ riêng. [Hai negative fixture trên auditor cuối](../tests/evidence/r09/t05-admin-faults-authoring-r1/audit-final-tests.json) đều bị từ chối đúng thiếu selector trước tạo output; relative/absolute paths cho JSON coverage byte-identical. Không phải hai lượt pilot thêm.

**Phân loại lỗi:** [R1/R2 FAIL giữ nguyên](../tests/evidence/r09/t05-admin-faults-authoring-r1/attempts.json). R1 bộ kiểm đọc nhầm `finality` thay `state`; sửa theo hợp đồng hiện hành, không đổi nghĩa UNKNOWN. R2 timeout30s chờ POST trong ca pointer, lỗi promise chưa được đón làm mất browser/matrix diagnostics; raw stderr ở logs/016.json, chưa có tọa độ r2 để chốt chính xác nguyên nhân đầu tiên. R3 cuộn/assert/ghi target vào viewport và gắn ngay hai outcome của promise, giữ deadline và rethrow lỗi vào chẩn đoán. R3 ghi CREATE y755.73/EDIT y982.52 trước cuộn, viewport720, sau cuộn y504; phù hợp giả thuyết click ngoài viewport r2 nhưng không coi tọa độ r3 là đo r2. Toàn bộ ca cuối PASS với ứng dụng/Kidea runtime nguyên byte. Auditor chỉ chuẩn hóa bốn path CLI được chọn, không tự suy root/ancestor; [hồi quy path](../tests/evidence/r09/t05-admin-faults-authoring-r1/audit-invocation-diagnostic.md) giữ tiêu chí. Không có lỗi ứng dụng/lõi Kidea mới được chứng minh trong lượt này.

**Giá trị cho Kidea:** kết luận bằng đúng request/intent/version/SQL, không chỉ text hay exit0; từ chối đánh giá đủ khi thiếu proof; lưu continuation qua public helper giữ gate/status/review hiện hành. Đây là kiểm chứng cách dùng và chất lượng bằng chứng, không tự gọi mỗi sửa pilot/harness là tính năng/lỗi runtime Kidea.

Public READ cuối WAITING; [public SAVE](../tests/evidence/r09/t05-admin-faults-authoring-r1/finalization-r1/resume-save-final-result.json) đã trả CONTINUATION_SAVED, xác minh bytes. [Kiểm bảo toàn](../tests/evidence/r09/t05-admin-faults-authoring-r1/finalization-r1/preservation.json) PASS: chỉ nextAction/checkpointRef đổi, mọi trạng thái/gate/owner/review giữ nguyên; không tự COMPLETE/approve. Pilot local commit `75853eacebd1abfbe8beb36b347356826b737209`, sạch và không có remote; [receipt](../tests/evidence/r09/t05-admin-faults-authoring-r1/finalization-r1/pilot-checkpoint-final.json). [Môi trường](../tests/evidence/r09/t05-admin-faults-authoring-r1/environment.json), [cleanup](../tests/evidence/r09/t05-admin-faults-authoring-r1/cleanup-final.json), [budget cùng baseline](../tests/evidence/r09/t05-admin-faults-authoring-r1/budget-final.json) được lưu. Không install/download/config host/cloud/deploy, không ghi đè Windows. MacIntel không là AppleSilicon PASS.

**Còn lại/Bước tiếp:** publish thất bại thật sau EDIT đã lưu, absent STATE, hai form/actor namespace HTTP đầy đủ; sau đó monitoring/observer, online backup/watermark/readiness và WQ. Release/restore đầy đủ thuộc T08, semantic maps/impacts/G2 và các task R09 sau vẫn mở. Không gom tất cả thành T05 hoặc tự nghiệm thu. Native Future chưa roadmap; AppleSilicon NOT_RUN; R10 chưa mở.

Task kế: nhánh publication failure/actor namespace còn thiếu, độ khó cao vì lịch sử/quan sát hiện tại, quyền và thời điểm lỗi. Đề xuất **GPT-5.6 Sol + High**, chưa cần Max mặc định; đây là đánh giá Codex, không cam kết chi phí tài khoản. High theo [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol), đã kiểm2026-09-18.

## Lịch sử — audit ma trận và regression quản trị, ngày 2026-09-18

Quyền trọn R09 và yêu cầu tiếp tục của Human áp dụng. Đã hoàn thành lát audit/bổ sung regression này; T05/`W-009-ADMIN-OPS` vẫn IN_PROGRESS, chưa khép137 nghĩa vụ, G2 hoặc nghiệm thu R09. Các phần dưới giữ lịch sử đúng nguồn cũ.

[Audit đủ137 nghĩa vụ](../tests/evidence/r09/t05-matrix-authoring-r1/coverage-audit-r1/coverage.md) giữ nguyên expected/source hash của từng dòng, đối chiếu với selector/hash bằng chứng đã chạy và ghi riêng phần thiếu. Kết quả thận trọng: {'PARTIAL': 96, 'NOT_RUN': 38, 'COVERED_FUNCTIONAL': 3}; bốn ca Q01–Q04 của amendment Max2 giữ riêng. Ba dòng AD-T02/03/10 COVERED_FUNCTIONAL chỉ nhận đúng nghĩa vụ chức năng trên stack đã kiểm; không là chứng nhận toàn ứng dụng hoặc Human approval. PARTIAL có bằng chứng thành phần/tích hợp nhưng chưa đủ mọi biến thể. NOT_RUN không được đổi thành PASS vì CTest có tên tương tự. ID ca trong bảng không là ID task roadmap: dòng T04 trong ma trận không phủ nhận nghiệm thu task R09-T04. Ledger T02 gốc không đổi byte; [JSON đầy đủ](../tests/evidence/r09/t05-matrix-authoring-r1/coverage-audit-r1/coverage.json) giữ cả yêu cầu gốc.

Đã bổ sung ba state × bốn trường qua UI thật, xác nhận đổi lịch có ACTIVE/Back/Escape, đủ chín ô STATE và state lạ, tạo trùng tên vẫn nhận ID khác, EDIT/STATE commit thật mất reply rồi reload/GET-only, và cuộc đua C10,N9→C9 khi đăng ký thứ10 thắng. Sửa Web: giữ kết quả lịch sử đã xác minh riêng với cảnh báo quan sát mới; BELOW_ACTIVE gắn đúng ô sức chứa, giữ draft; mất identity thật xóa cả kết quả/lỗi, callback refresh cũ không thêm nội dung sau đổi generation. Không suy lỗi field cụ thể từ INVALID_FIELDS chung của server.

- Web nguồn cuối `2607aa618c07bfbb7c2bac5a9f063944289dc27a`: [receipt/patch](../tests/evidence/r09/t05-matrix-authoring-r1/source-final-r2.json), [năm gate cuối](../tests/evidence/r09/t05-matrix-authoring-r1/web-r2/checks-final.json) đều PASS:93unit, check0 lỗi/0 cảnh báo, lint/build,18SSR–native Chrome Mac với API fixture. Không bỏ ca/skip.
- [HTTPS/WSS cuối](../tests/evidence/r09/t05-matrix-integration-r6/run/browser/result.json) **71/71 PASS**, giữ đủ62 kiểm cũ, thêm9 kiểm ma trận. Backend thật/Caddy TLS/Docker Chromium153.0.8010.12, không bypass TLS. Lost reply và thời điểm registration trước admin POST được điều khiển riêng; các mutation/kết quả lưu trên backend thật. Không nhận pacing chức năng là offered-load WQ.
- [SQL audit sau backend dừng](../tests/evidence/r09/t05-matrix-integration-r6/run/browser/matrix-audit.json) PASS: 57 intent quản trị/15 workshop thay đổi thực; mỗi intent đúng một durable result/audit, APPLIED delta đúng trường, NO_CHANGE/REJECTED không delta; version/outbox liên tục chỉ cho thay đổi thực, không event cho noop/từ chối/GET. Audit không có marker token/CSRF/nội dung riêng giả; C10,N10 được giữ đúng. Chỉ là kiểm SQL sau shutdown, không crash/restore.
- [Runner cuối](../tests/evidence/r09/t05-matrix-integration-r6/run/result.json) PASS theo [manifest](../tests/evidence/r09/t05-matrix-integration-r6/manifest.json); xác minh toàn source/plan/dependency/build trước và sau, dọn tài nguyên sở hữu rồi mới kiểm SQL. [112 nguồn backend](../tests/evidence/r09/t05-matrix-authoring-r1/backend-source-audit.json) vẫn đúng artifact1fb4a69 bốn preset cũ; không nhận là build/sanitizer backend mới hoặc mở EX ngoài SQLite/WAL r2 đã chốt.

[FAIL/retry](../tests/evidence/r09/t05-matrix-authoring-r1/attempts.json) giữ nguyên raw và frozen plan riêng: r1 timeout thông báo lưu sau backend UPDATED, không đủ receipt để xác định nguyên nhân cụ thể; r2 ghi nhận POST429 thật trong pha thao tác liên tiếp. Thêm1100ms giữa từng thao tác chức năng, giữ trần20/s burst40 và oracle200; không retry ngầm hoặc giảm kiểm admission. R3 đi qua mọi ma trận trước race rồi FAIL vì thông báo BELOW_ACTIVE mất; [SQL chẩn đoán](../tests/evidence/r09/t05-matrix-integration-r3/race-diagnostic.json) xác nhận backend C10,N10 đúng, phân tích accept→refresh→observe thấy message bị thay và chưa gắn lỗi capacity. Sửa nguyên nhân rồi chạy lại đầy đủ Web. R4 FAIL ở oracle cảnh báo xung đột vì kết quả lịch sử/cảnh báo hiện chung một paragraph; giữ DOM thật, tách hai vùng trạng thái và giữ nguyên oracle. R5 FAIL do barrier cũ chờ text thành công lịch sử rồi đọc POST trước đó; thay bằng response POST cùng intentId/FINAL marker và fieldset hết busy, giữ mọi oracle domain/UI và số POST. Web cuối2607 đã qua đủ năm gate; nguồn Web không đổi sau r5. R6 kiểm lại toàn bộ HTTPS/WSS và SQL audit đều PASS. Raw FAIL không đổi thành PASS.

[Public REVISE revision5](../tests/evidence/r09/t05-matrix-authoring-r1/finalization-r1/review-revise-r5-result.json) REVIEW_RECORDED/DRAFT, so đủ103 fixed version trước; chỉ AdminForm đổi ý nghĩa, nguồn cũ khác nguyên byte. Giữ10 owner/impact, lịch sử nghiệm thu T04 và mọi gate; không ghi Human approval hoặc DONE. [READ cuối](../tests/evidence/r09/t05-matrix-authoring-r1/finalization-r1/resume-read-final-result.json) WAITING/IMPACT_REVIEW_REQUIRED; [SAVE](../tests/evidence/r09/t05-matrix-authoring-r1/finalization-r1/resume-save-final-result.json) CONTINUATION_SAVED. Metadata chỉ qua public helper. Pilot checkpoint local `e81d23434f8ba92bfee17e855ce3cd58b4f199ae`, sạch/không remote; [delta và byte receipt](../tests/evidence/r09/t05-matrix-authoring-r1/finalization-r1/pilot-checkpoint-delta.json) bảo toàn nguồn/checkpoint/bằng chứng.

[Budget](../tests/evidence/r09/t05-matrix-authoring-r1/budget-final.json) cùng baseline: mới14.17GiB/20GiB, free322.6GiB, dự phòng Git128MiB; [cleanup](../tests/evidence/r09/t05-matrix-authoring-r1/cleanup-final.json) không container active/network R09. Không tải/cài/config host/cloud/deploy. Core293 dùng lại vì engine không đổi; không nhận lượt core mới. Ảnh kết quả race đã xem; không nhận đây là kiểm đủ mọi viewport/zoom.

Bước tiếp: khép nhánh publish thất bại thật sau khi đã lưu nội dung, GET-not-found còn UNKNOWN, double-click cả ba action, reverse race giảm sức chứa và actor-namespace literal ID qua HTTP; sau đó monitoring/observer, online backup/watermark/readiness/restore và đo WQ/percentile/queue/RSS. Semantic maps/impact, các task R09 sau, T08 Human PROD, G2/nghiệm thu cuối còn mở. Android/iOS Future chưa roadmap, Apple Silicon NOT_RUN, R10 chưa mở.

Độ khó task kế tiếp cao vì concurrency, ý định UNKNOWN và phạm vi actor phải được kiểm chính xác. Đề xuất **GPT-5.6 Sol + High**: đủ chiều sâu cho lát hữu hạn, chưa cần XHigh/Max; đây là đánh giá của Codex. High được hỗ trợ theo [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol); không suy giá/usage tài khoản Codex từ giá API.

## Lịch sử — danh sách trực tiếp và `/me`, ngày 2026-09-18

Quyền trọn R09 và yêu cầu tiếp tục của Human áp dụng. T04 đã nghiệm thu; T05/`W-009-ADMIN-OPS` vẫn IN_PROGRESS, chưa hoàn tất137 nghĩa vụ/G2 hoặc nghiệm thu R09. Các phần dưới là lịch sử đúng nguồn cũ.

Đã nối WSS cho danh sách public/admin và trang đăng ký riêng `/me`. Public không nhận DRAFT hay ID đăng ký riêng; admin nhận số đăng ký hiệu lực. Mỗi audience xác minh identity/epoch/generation riêng, subscribe trước GET đối chiếu và giữ high-water đúng miền version. `/me` xác minh lại phiên sau hydrate trước khi hiện dữ liệu riêng, giữ CANCELLED trước GET cũ; lỗi đọc phiên tạm thời ẩn dữ liệu rồi chỉ phục hồi cùng identity, mất quyền thật xóa dữ liệu. Không tự POST khi reconnect.

Danh sách public rỗng và anonymous chưa có metadata epoch: dùng HTTP bootstrap chỉ đọc, có deadline và dừng theo vòng đời trang, cho tới khi có epoch thật; sau đó nối WSS. Không tạo epoch giả hoặc nhận trạng thái rỗng này là OBSERVED trực tiếp.

Web nguồn cuối `fa366db49917c8f0699d4d911bbd09762889a570`: [receipt và patch](../tests/evidence/r09/t05-pages-authoring-r1/source-final.json). Backend dùng nguyên artifact source `1fb4a69f9faf69893440d43fdc8648347074e715`: [112 nguồn khớp](../tests/evidence/r09/t05-pages-authoring-r1/backend-source-audit.json). Không nhận đây là lượt build/sanitizer backend mới; bằng chứng bốn preset và giới hạn EX r2 của lát trước còn đúng artifact.

- [Web cuối](../tests/evidence/r09/t05-pages-authoring-r1/checks-r4.json):93 unit PASS, check0 lỗi/0 cảnh báo, lint/build PASS. [18 SSR–native Chrome Mac](../tests/evidence/r09/t05-pages-authoring-r1/test-server-r4.stdout.log) PASS dùng API fixture, không thay kiểm backend/WSS thật.
- [HTTPS/WSS cuối](../tests/evidence/r09/t05-pages-integration-r2/run/browser/result.json):**62/62 PASS**, giữ đủ52 kiểm cũ và thêm10 kiểm cho ba trang. Backend thật/Caddy TLS/Docker Chromium153.0.8010.12, không bypass TLS. Các mutation publish/register/cancel/đổi nội dung và thu hồi phiên/quyền là thật; GET lịch sử cũ và session503 được tiêm riêng, không gộp thành lỗi backend thật.
- [Runner](../tests/evidence/r09/t05-pages-integration-r2/run/result.json) PASS, xác minh nguồn/plan/dependency/build cả trước và sau, dọn container/network sở hữu. [Manifest](../tests/evidence/r09/t05-pages-integration-r2/manifest.json) cố định; r1 chỉ chuẩn bị, NOT_RUN_SUPERSEDED.
- Đã xem trực tiếp ảnh danh sách public/admin và `/me`360px của lượt cuối, không tràn ngang. Chỉ nhận viewport đã kiểm, không mọi viewport/zoom. Các probe quota128/socket và mutation<5s giữ nguyên phạm vi chức năng, không chứng minh workload/percentile/queue/RSS.

Giữ nguyên raw FAIL/các retry trong [authoring-r1](../tests/evidence/r09/t05-pages-authoring-r1/attempts.json): Svelte cảnh báo capture initial; Node24 strip mode không hỗ trợ parameter properties; fixture chưa forward browser API; callback fixture còn chạy sau context close dù18 case đã đạt. Sửa nguyên nhân: derived initial fallback, constructor explicit fields, forward fake API và chờ route callbacks kết thúc trước khi đóng context. Kiểm lại cả năm gate cuối, không bỏ ca, nuốt lỗi async hoặc hạ kiểm an toàn. Không có frozen full-source snapshot của lượt authoring FAIL đầu; không nhận log đó là manifest hoàn chỉnh. READ khởi chạy nền đầu không hoàn tất được ghi riêng; READ r2 hoàn tất trước commit, không suy nguyên nhân process dừng.

Còn lại: đối chiếu ma trận AD/E/AR/OP/QT và137 nghĩa vụ, thêm ca thiếu; workload/percentile/queue/RSS, telemetry/observer độc lập, online backup/watermark/readiness/restore; đối chiếu ý nghĩa ba bản đồ/impact và các gate R09 sau. T08 Human PROD và nghiệm thu cuối thuộc Human; native Future, Apple Silicon NOT_RUN, R10 chưa mở. Kidea core293 của lát trước là bằng chứng dùng lại do engine không đổi, không phải lượt core mới.

[Public REVISE revision4](../tests/evidence/r09/t05-pages-authoring-r1/finalization-r1/review-revise-r4-result.json) ghi REVIEW_RECORDED/DRAFT, giữ owners và lịch sử nghiệm thu T04. [Public READ cuối](../tests/evidence/r09/t05-pages-authoring-r1/finalization-r1/resume-read-final-result.json) trả WAITING với IMPACT_REVIEW_REQUIRED; không tự ghi approval, DONE hoặc hoàn tất impact. Metadata chỉ được viết bằng public helper. [Public SAVE](../tests/evidence/r09/t05-pages-authoring-r1/finalization-r1/resume-save-final-result.json) trả CONTINUATION_SAVED, giữ status/gate/blocker/return stack và điểm tiếp tục đúng phần còn thiếu.

Pilot checkpoint local `ccd2b9b575e20f64453fd6aadc329465e5127d82`, Git sạch/không remote; [receipt cuối](../tests/evidence/r09/t05-pages-authoring-r1/finalization-r1/pilot-checkpoint-final.json). [Delta nguồn/checkpoint công khai](../tests/evidence/r09/t05-pages-authoring-r1/finalization-r1/pilot-checkpoint-delta.json) giữ các metadata/evidence đã tạo; mã Web vẫn đúng sourcefa366 đã kiểm. [Cleanup](../tests/evidence/r09/t05-pages-authoring-r1/cleanup-final.json) không còn container active/network R09.

Bước tiếp theo: audit ma trận T05 và bổ sung regression còn thiếu theo ca cụ thể. Độ khó cao vì liên quan concurrency, crash/audit, phiên/epoch và phân biệt bằng chứng thành phần/tích hợp. Đề xuất **GPT-5.6 Sol + High**: đủ chiều sâu đối chiếu và sửa ca khó trong phạm vi hữu hạn; chưa cần tăng tới XHigh/Max. Đây là đánh giá task của Codex; model có mức High theo [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-5.6-sol), không là cam kết chi phí Codex theo giá API.

## Lát outbox/detail/form — lịch sử nguồn Web9506, ngày 2026-09-18

Quyền thực thi trọn R09 giữ nguyên. T04 đã được Human nghiệm thu; T05 và `W-009-ADMIN-OPS` vẫn IN_PROGRESS. Không coi các kiểm dưới đây là toàn bộ ma trận T05, G2 hoặc nghiệm thu R09.

Backend C++ đã thêm outbox worker và WSS public/admin/private. Snapshot lấy dữ liệu HTTP có thẩm quyền, không cộng/trừ số chỗ ở client; no-op không tạo event. Xác minh phiên/quyền/CSRF/Origin/epoch trước mỗi lần phát; lỗi storage giữ nghĩa vụ pending và đóng kênh. Receipt ACK ngẫu nhiên chỉ giải phóng credit, không xác nhận nghiệp vụ. Giữ trần 128 socket toàn cục, 8/actor, 16 khách/peer TCP, 8 frame chưa ACK và 1MiB; heartbeat 2s, frame client 8KiB, 5/s burst10. Với proxy hiện hành, peer TCP là proxy, không tin địa chỉ do client khai.

Web detail, form quản trị và phần đăng ký riêng trên detail dùng WSS, subscribe trước GET đối chiếu; mất tín hiệu quá 5s hiện UNKNOWN và giữ lần quan sát trước. Không tự gửi lại mutation khi reconnect. Thu hồi quyền/đổi danh tính vô hiệu hóa response cũ và nội dung riêng. Kiểm đơn vị giữ high-water từng workshop/actor/epoch, snapshot cũ/lặp, cùng version mâu thuẫn, batch sai và CANCELLED không sống lại. **Trang danh sách public/admin và trang `/me` chưa được nối cập nhật trực tiếp.**

Nguồn backend build cuối `1fb4a69f9faf69893440d43fdc8648347074e715`; Web cuối `9506bcd805b9bbcc3910f5a9f4979c9576c54e5f`. [Bản vá nguồn cố định](../tests/evidence/r09/t05-events-authoring-r1/source-final-r3.json) và patch tương ứng giữ cả thay đổi backend/Web trong pilot Git local không remote. Không có dependency mới hoặc cài đặt hệ thống.

### Bằng chứng trên nguồn cuối

- [Build r4](../tests/evidence/r09/t05-events-build-r4/run/results.json): cả bốn preset dev, ASan/UBSan, TSan, release thành công. Giữ đầy đủ 50 CTest/856 assertion cũ và HTTP18/session8/shutdown2 mỗi preset; thêm 77 assertion updates mỗi preset. Format/tidy giữ nguyên chính sách. TSan cũ vẫn có EX r2 SQLite/WAL đã nghiệm thu; không gọi toàn backend TSan sạch, không mở ngoại lệ cho phần mới.
- [Transport component r3](../tests/evidence/r09/t05-socket-component-r3/result.json): 8 kiểm hoạt động qua socket/backend thật mỗi preset, cả bốn PASS. Phần mới chạy TSan với halt-on-error, không EX. Đây là loopback trong Docker network-none, không thay kiểm TLS/Chrome hay tải.
- [Web checks cuối](../tests/evidence/r09/t05-events-web-r1/checks-final-4.json): 81 unit, check không lỗi/cảnh báo, lint và build PASS. [18 SSR–Chrome Mac](../tests/evidence/r09/t05-events-web-r1/server-final-4.stdout.log) dùng API fixture; không gọi đây là backend/WSS tích hợp.
- [HTTPS/WSS r10](../tests/evidence/r09/t05-events-integration-r10/run/browser/result.json): **52/52 PASS** với backend release thật, Caddy TLS và Docker Chromium153.0.8010.12; không bypass TLS. [Runner/cleanup và xác minh nguồn cuối](../tests/evidence/r09/t05-events-integration-r10/run/result.json) PASS theo [manifest cố định](../tests/evidence/r09/t05-events-integration-r10/manifest.json). Bao gồm authority/audience, subscribe-before-GET, heartbeat, blackout >5s, reconnect không POST replay, thu hồi participant/admin thật, phục hồi HTTP429/503 được tiêm riêng, frame/actor/guest/global limits và writer no-op có kết quả durable khi đủ128 socket/reader hết8 frame credit. Các probe này không chứng minh throughput/percentile/queue/RSS.
- Ảnh public/admin360 của r10 đã xem trực tiếp: không tràn ngang, trường/nút đầy đủ. Không nhận đây là kiểm mọi viewport/zoom. Kidea core293/293, không skip; [biên nhận](../tests/evidence/r09/t05-events-authoring-r1/core/summary.json).
- [Bảo toàn byte khi checkout qua Git](../tests/evidence/r09/t05-events-authoring-r1/git-byte-portability.json): probe core.autocrlf=true vẫn giữ CRLF nguyên gốc ở evidence R09 và snapshot pilot; control không bảo vệ phát hiện chuyển LF. Không thay cấu hình Git trên máy.

### FAIL và retry được giữ nguyên

Build r1 dừng sau khi phát hiện lỗi phân loại storage503 thành thiếu quyền; sửa phân biệt lỗi storage và giữ pending. Build r2 FAIL vì biến test-root/sanitizer chưa truyền từ wrapper cha tới bài updates; sửa truyền biến, không bỏ kiểm. Build r3 dừng sau khi tidy bắt copy Json không cần thiết; sửa const reference rồi chạy lại toàn bộ r4. Các output/manifest/raw FAIL nằm riêng `t05-events-build-r1`–`r3`; không ghi đè.

Transport r1 timeout ở barrier snapshot ban đầu; không có raw message nên **chưa chứng minh nguyên nhân timeout**. R2 chỉ thêm chẩn đoán, giữ oracle và cả bốn PASS. R3 sửa oracle theo hợp đồng at-least-once: chấp nhận snapshot tương đương lặp, kiểm bắt buộc có snapshot, đúng thứ tự SUBSCRIBED trước SNAPSHOT và toàn bộ nội dung canonical ban đầu. Giữ r1/r2 và source harness đúng manifest; không miễn kiểm an toàn. Web giữ các check/tidy FAIL và các lượt SSR trước riêng biệt.

HTTPS r1/r2 FAIL ở chuỗi quota: nút bị thay khỏi DOM, r2 ghi nhận UI đã ẩn phiên riêng; không có status/body kiểm phiên ở hai lượt này nên không gán nguyên nhân HTTP cụ thể. R3 bổ sung quan sát response, kiểm phiên đều 200 nhưng một GET lịch sử trả **429**, giữ raw assertion FAIL. Phân tích mã cho thấy UI đang gộp mọi lỗi đọc phiên vào mất quyền; sửa phân biệt lỗi tạm thời với 401/403 hoặc metadata sai. Giữ giới hạn 20/s burst40, đóng các trang fixture cũ đã kiểm xong và chèn khoảng nghỉ có ghi rõ giữa các pha quota chức năng; không retry ngầm GET, không đổi kỳ vọng 200 hoặc giảm kiểm admission. Kiểm tải vẫn riêng. Thêm ca tiêm HTTP429/503 cho participant/admin, phục hồi chỉ đọc và giữ draft; kiểm SQL thu hồi phiên thật vẫn bắt buộc. Chỉ nhận các ca mới đạt khi kết quả HTTPS cuối xác nhận.

R4 xác nhận participant phục hồi 429/503 và admin429, rồi bắt lỗi lớp bảo vệ admin hủy form khi nhận 503. Sửa lớp này: lỗi tạm thời tạm ẩn/inert phần riêng nhưng giữ component/draft, chỉ hiện sau kiểm đúng identity; 401/403, role/actor/epoch sai hoặc metadata không hợp lệ vẫn hủy nội dung. R5 đạt các ca phục hồi này nhưng reader control gặp JSON ghi dở; đổi cả request/result sang ghi temp rồi rename trong cùng thư mục, không bỏ xác minh control. R6 đạt blackout/reconnect nhưng đọc HTML đúng lúc navigation sau revoke; sửa barrier thành 401 thật, dòng riêng detached và trang yêu cầu phiên hợp lệ. Diagnostic body promise chờ trước browser.close gây timeout cleanup r6; đóng browser trước khi chờ các observation. Manifest r7 chỉ được chuẩn bị, **NOT_RUN_SUPERSEDED**, không FAIL/PASS sản phẩm. Plan r6 thay sau khi browser đã ghi FAIL nhưng runner chưa dọn xong; [timing receipt](../tests/evidence/r09/t05-events-integration-r6/source-timing.json) ghi rõ, không nhận source unchanged cho lượt đó. Nguồn sản phẩm của r5/r6 không đổi.

HTTPS r8 giữ nguyên nguồn ứng dụng và vẫn FAIL khi đọc JSON control đang đổi, dù hai phía đã dùng rename. Không suy nguyên nhân filesystem cụ thể từ lỗi parse. R9 đổi result sang tên riêng theo sequence, SHA-256 framing trước JSON, kiểm id/action/status/schema và giữ mọi quan sát chưa đủ byte; chỉ chờ trong deadline control cũ. [Kiểm regression](../tests/evidence/r09/t05-events-authoring-r1/control-receipt-tests.stdout.log) bao gồm mọi prefix thiếu byte, corruption, sai sequence/action/schema. Không sửa mutation, quota, deadline hoặc kỳ vọng sản phẩm.

HTTPS r9 đạt 47 kiểm, gồm blackout/reconnect/thu hồi phiên/quyền thật và đủ 128 socket, rồi FAIL vì harness dùng API context đã đóng khi dọn trang cũ. Sửa tạo context A mới chỉ để các writer probe có authority hiện hành; không mở socket mới, không gửi lại ý định cũ, không bỏ hai probe hoặc đổi kỳ vọng. HTTPS r10 là lượt retry thuộc R09, kiểm lại toàn bộ trên nguồn ứng dụng không đổi; không mở phase R10.

### Lưu và bàn giao

Pilot checkpoint local `2b173616d6cf62cf86b6225ecdbd53c1c43215ed`, Git sạch/không remote; mã runtime vẫn đúng backend1fb4a69/Web9506 đã kiểm. [Patch checkpoint nén lossless](../tests/evidence/r09/t05-events-authoring-r1/pilot-checkpoint-final-gzip.json) xác minh roundtrip và SHA nguyên gốc; các raw FAIL/PASS không đổi. [4.566 blob Git](../tests/evidence/r09/t05-events-authoring-r1/git-staged-byte-audit.json) khớp từng byte. Code/docs sạch whitespace; [67 cảnh báo của raw evidence](../tests/evidence/r09/t05-events-authoring-r1/staged-whitespace-review-final.json) giữ nguyên, không trim log/snapshot để lấy sạch.

[Budget cuối](../tests/evidence/r09/t05-events-authoring-r1/budget-final.json) dùng cùng baseline: mới14.818GB (~13,8GiB)/20GiB, free~323GiB, dự phòng Git128MiB. [Cleanup](../tests/evidence/r09/t05-events-authoring-r1/cleanup-final.json) không còn container active/network R09. Không tải/cài/cấu hình host hoặc tạo cloud workload.

### Còn lại của T05/R09

1. Nối và kiểm các trang danh sách/lịch sử còn thiếu; bổ sung ma trận AD/E/AR/OP/QT còn chưa có chứng cứ, gồm concurrency, audit/crash, epoch/restore và lỗi server. Không nhận 137 nghĩa vụ toàn pilot đã PASS.
2. Đo workload/percentile, queue/RSS và chất lượng đầu–cuối theo profile đã duyệt. Một mutation dưới 5s hoặc kiểm quota không thay bài tải.
3. Telemetry/observer độc lập, SQLite online backup, watermark/verified destination, readiness và restore. Docker cùng Mac không chứng minh mất cả Mac; cloud chỉ trong quyền/budget còn hiệu lực.
4. T08 giữ Human PROD lab; T09/T10 sau release/hotfix, T11–T14 và G2/nghiệm thu cuối còn mở. Android/iOS Future chưa roadmap, Apple Silicon NOT_RUN, R10 chưa mở.

Review T04 đã chấp thuận được giữ làm lịch sử đúng source. [Public REVISE revision3](../tests/evidence/r09/t05-events-authoring-r1/finalization-r1/review-revise-r3-result.json) gắn mã/evidence hiện hành, trạng thái DRAFT; không tạo Human approval mới hoặc tự hoàn tất impact. [Public READ](../tests/evidence/r09/t05-events-authoring-r1/finalization-r1/resume-read-final-result.json) trả WAITING với IMPACT_REVIEW_REQUIRED, không blocker; W-009-ADMIN-OPS vẫn IN_PROGRESS. Source đã gắn đúng byte không thay đối chiếu nghĩa vụ/ý nghĩa trong ba bản đồ. Metadata chỉ cập nhật bằng public helper, không sửa `.kidea` thủ công. [Public SAVE](../tests/evidence/r09/t05-events-authoring-r1/finalization-r1/resume-save-final-result.json) trả CONTINUATION_SAVED; giữ status/gate/blocker/return stack và điểm tiếp tục đúng phần còn mở.

## Checkpoint quản trị trước lát WSS — lịch sử

Ngày 2026-09-18. T04 đã được Human nghiệm thu bằng “phê duyệt T04 và làm tiếp nhé”. Public APPROVE/CLOSE/COMPLETE thành công; SELECT/START đưa `W-009-ADMIN-OPS` tới IN_PROGRESS. Không có yêu cầu duyệt lại quyền thực thi từng lượt; [quyền trọn R09](R09_EXECUTION_AUTHORITY.md) giữ nguyên.

## Đã triển khai trong pilot local

Trang danh sách/tạo/sửa dành cho admin, quyền xác minh phía server; bản nháp không ra public. Mỗi ý định lưu mã và nội dung trước một POST; mất phản hồi/tải lại chỉ GET cùng mã. ID bản tạo lấy từ kết quả có thẩm quyền, không tìm theo tên.

Sửa chỉ gửi trường đã đổi; lịch là một nhóm. Đổi trạng thái tách khỏi lưu nội dung. Giảm sức chứa/đổi lịch có đăng ký/đổi trạng thái cần xác nhận. Khi đã phát hiện nguồn thay đổi, giữ phần nhập và yêu cầu đối chiếu; khi chấp nhận bản mới chỉ giữ trường người dùng đã sửa, không ghi đè các trường của người khác. Cùng phiên bản khác nội dung khóa mutation tới khi đọc đối chiếu hợp lệ. Đổi actor/quyền ẩn nội dung riêng và không lộ kết quả cũ.

Có [bản vá Web cố định](../tests/evidence/r09/t05-admin-r1/source-patch.json) để đối chiếu nguồn ngoài repo. Nguồn pilot `05d4895cfa8f6b13e6fe1a0745f7a7dd8504f6e4` thêm admin; nguồn cuối xem [môi trường/source receipt](../tests/evidence/r09/t05-admin-r1/environment-final.json). Pilot chỉ Git local, không remote. Backend release T04 `c168eea24192827fe4545368164c0e38e42b4e27` được dùng nguyên artifact, không build lại hoặc đổi backend trong lát này.

## Bằng chứng

- 63 unit PASS, kiểm kiểu không lỗi/cảnh báo, lint và build PASS: [checks-3](../tests/evidence/r09/t05-admin-r1/checks-3.json).
- 18 SSR–Chrome Mac PASS trên nguồn cuối: [log](../tests/evidence/r09/t05-admin-r1/server-3.stdout.log). Lượt server-2 chỉ có 11 SSR vì chưa đặt biến chạy browser; không nhận là Chrome.
- HTTPS/backend thật r1/r2 FAIL: bài kiểm nhập ô Tên trước khi chuyển từ trang tạo sang sửa xong. Chỉ chờ reactive chưa đủ; r3 chờ đúng URL và heading trang sửa, giữ nguyên assertion khóa nút và toàn bộ kiểm cũ, 28 PASS.
- HTTPS/backend thật r4: **29/29 PASS trên nguồn cuối**, [kết quả](../tests/evidence/r09/t05-integration-r4/run/browser/result.json), [manifest cố định](../tests/evidence/r09/t05-integration-r4/manifest.json). Không bỏ kiểm, không bypass TLS. Harness giữ 18 kiểm cũ và thêm quản trị, có trường hợp tiêm phản hồi GET mâu thuẫn (phải phân biệt với phản hồi backend thật).
- Giữ log check/lint FAIL ban đầu: kiểu route mới chưa được sync; ESLint bắt kiểu RequestInit và each chưa key. Đã sync bằng dependency sẵn có và sửa mã, không giảm kiểm.
- Ảnh quản trị 360px của r3 đã xem trực tiếp: không tràn ngang, trường nhập/nút đầy đủ. Không phải kiểm đủ mọi viewport/zoom/admin case.

## Còn phải thực hiện, không coi T05 hoàn tất

1. Hoàn thiện và kiểm ma trận AD-T01–15, lỗi field từ server, concurrency, audit/crash và các trường hợp chưa có bằng chứng; các ca hiện tại không thay 137 nghĩa vụ toàn pilot.
2. Outbox worker, WSS đúng audience/phiên/epoch, queue hữu hạn và auth trước mỗi lần phát; heartbeat, reconnect, stale/unknown và đo độ trễ. Poll đọc form hiện tại chưa thay hợp đồng WSS.
3. Telemetry/observer độc lập, SQLite online backup, watermark/verified destination, readiness và restore; kiểm cục bộ không chứng minh mất cả Mac. Chỉ xin quyền khi thực sự cần workload/cloud/cấu hình ngoài quyền hiện hành.
4. T08 Human PROD lab, T09/T10 thay đổi sau release/hotfix, T11–T14 và G2/nghiệm thu cuối vẫn mở. Android/iOS Future, Apple Silicon NOT_RUN, phase R10 chưa mở.

Giới hạn TSan EX r2 đã nghiệm thu ở T04 giữ nguyên, không đổi thành TSan sạch. Chưa deploy hay cài công cụ hệ thống.

Ngân sách sau kiểm: phát sinh cộng dồn 11.18 GB/21.47 GB (byte thập phân), host còn 351.46 GB; baseline giữ nguyên. Không còn container/network kiểm sở hữu đang chạy. `git diff --check` chỉ báo whitespace trong hai raw log FAIL; giữ nguyên byte bằng chứng, tài liệu/code viết mới sạch. Xem [budget](../tests/evidence/r09/t05-admin-r1/budget-final.json), [audit checkpoint](../tests/evidence/r09/t05-admin-r1/checkpoint-audit.json).
