# R02-T02 — Mẫu cấu trúc và đối chiếu, 2026-09-12

Nguồn hợp đồng: [S06-r1 tại 207cb5d](https://github.com/Kynderis/kidea/blob/207cb5dce8620521f75efee1a393353c9cf6ea24/KIDEA_DESIGN.md#schema-fields-proposal), Human “Duyệt nhé” sau answer cùng bản. [Tracker](../../KIDEA_ROADMAP.md#work-state) giữ trạng thái công việc; tài liệu này chỉ giữ bằng chứng và giới hạn.

## Đầu vào và phạm vi

- Node local Windows x64 `24.21.0`, cùng runtime đã chuẩn bị ở T01; không cài dependency mới hoặc đổi PATH/global.
- [Bộ mẫu](../fixtures/r02-t02/README.md) gồm 4 file baseline: INDEX/work/review, nguồn sản phẩm Unicode ngoài `docs/`; 5 Item, một nhóm chưa phân rã và một nhóm dở. Manifest có 41 biến thể và expected riêng, gồm 33 REJECT.
- Đầu tiên phác mẫu gồm mười bước nhưng các bước xa chưa có gói gate cụ thể. Rà ngữ nghĩa đã thu về lát cắt 5 Item với phạm vi/gate rõ, rồi cập nhật recipe và kiểm tra lại. Không nhận mẫu ban đầu là init đầy đủ hợp lệ hoặc sửa hợp đồng để giữ mẫu.
- Không gọi helper để thực hiện hồ sơ, không có phiên AI mới, không tạo pilot/release hoặc ghi/xóa file ngoài các nguồn test/tài liệu của repo. Mẫu giả có nhãn APPROVED hoặc vận hành không là xác nhận/bằng chứng thật.

## Kiểm tra recipe — không là chạy validator

Lệnh từ root: `./.tools/node-v24.21.0-win-x64/node.exe tests/fixtures/r02-t02/check-recipes.mjs`.

Expected: ID mẫu duy nhất, đích mutation có thật trong baseline, thay văn bản thô đúng một chỗ, dựng được mọi biến thể trong map bộ nhớ, không đổi baseline. Actual: exit 0; `materializedInMemory: 41`, 4 VALID_STRUCTURE, 1 STRUCTURE_ONLY_NOT_AUTHORIZATION, 1 STRUCTURE_ONLY_NOT_DEPLOYED, 1 INSUFFICIENT_FOR_COMPLETION, 1 INSUFFICIENT_FOR_DEPLOYMENT, 33 REJECT. Đây là phân loại expected trong manifest, **không phải actual từ Kidea**. `kideaValidatorRun: false`.

Kiểm tra bổ sung chỉ đọc: đối chiếu SHA-256/tập 7 file bộ mẫu trước/sau gọi script — không đổi. Đọc ba khối JSON baseline, tìm 21 Ref và đối chiếu file/anchor đích trong root fixture — đủ đích. Không áp phép kiểm tra link thường này lên path cố ý sai trong các biến thể. Không coi link đủ là ngữ nghĩa/approval đã đạt.

## Đối chiếu nội dung S04

| Căn cứ | Mẫu đã đối chiếu | Kết luận ở mức tài liệu |
|---|---|---|
| KA-03/09 — status/nguồn | S-01–04, E-01–12/20–30 | Một nguồn mỗi Item, một currentItemId; không chọn record cuối, không dùng văn xuôi hoặc sửa nguồn ngầm để che thiếu |
| KA-11 — giữ việc dở | S-01/03/04, E-22 | W-012 vẫn IN_PROGRESS có returnStack; chỉ W-013 hiện hành. Chọn nhóm/null không xóa tiến độ; các mục dở khác được giải thích |
| KA-25 — cây và tiến độ | S-01/02/07, E-13–18/23 | DONE còn truy được; nhóm partial/empty không hoàn tất; cha/vòng/LEAF/con và chuyển plan được thể hiện riêng |
| KA-06/07 — giới hạn approval | S-05, E-19/32 | Có confirmationRef giả chưa là duyệt hợp lệ; thiếu xác nhận/đích gate phải lộ rõ. T03/T04 còn cần hợp đồng hiệu lực |
| KA-13/25/28 — bản triển khai | S-06/08, E-33 | Ref release/revision không là đã deploy; ví dụ backend lỗi không thành toàn bộ đạt. Dữ liệu giả/vắng nguồn không chứng minh chưa triển khai |
| KQ-02/03/05/06/10 — trung thực | Toàn manifest + kiểm tra chỉ đọc | Giữ ranh giới expected/actual, cấu trúc/ngữ nghĩa và mẫu/quan sát thật; chưa tính KA/KQ PASS |

Các mẫu sai có thể vi phạm nhiều ràng buộc liên quan (ví dụ trùng ID làm currentItem mất đích); reason nêu lỗi chính, không ép validator chỉ được báo đúng một lỗi. Trường hợp JSON khóa trùng phải được T05 phát hiện bằng cơ chế đã chọn; việc JavaScript đọc JSON thường không chứng minh đã bắt lỗi này.

## Hồi quy khung đã có

Lệnh `./.tools/node-v24.21.0-win-x64/node.exe --test tests/helper.test.mjs`: 16/16 pass, 0 fail/skip. Sáu action sản phẩm vẫn NOT_IMPLEMENTED, không đọc hồ sơ; không sửa skill/helper/test khung. Đây là hồi quy khung, không là 41 test schema chạy đạt hoặc nghiệm thu T02 runtime.

## SHA-256 bộ mẫu lúc kiểm tra cuối

Hash byte local lúc kiểm tra; newline checkout khác có thể làm hash khác. Dùng đúng bản nguồn và đối chiếu nội dung, không coi hash là chứng minh ngữ nghĩa.

| File trong `tests/fixtures/r02-t02/` | SHA-256 |
|---|---|
| check-recipes.mjs | ff5e031790763dd4a91d380eb1f98cfe681efe9c11166028910781d6e06a0319 |
| manifest.json | 6f219d7c29ddfdf8911094e3e923d39465f5f82bec825b76d819667187a7ceda |
| README.md | 73b749aafc065c9882a1c0379df5af1154100605ed84bd418b66949ec4e4547a |
| base/.kidea/INDEX.md | 09bb700bcf28db91c48edae68a72ce6078e36dc1fb1bf09e822903713b2cac41 |
| base/.kidea/work.md | e3f97bdfce2617e1be13e3082eb11365b3b66a26b3e81eef3dc5b11fb0f6ebb4 |
| base/.kidea/reviews/RV-001.md | c688650ff2f4898a8090f65708a58a02059ed6aa0a84a02189188213d02a6a58 |
| base/tài liệu/đặc tả.md | ae4950cf38ffdf00236c6d6e0348dc67c251a6f3145fe862904466bf6ccf6113 |

## Giới hạn và cleanup

T02 chỉ có hợp đồng và fixture; T03/T04 còn approval/N/A/fingerprint/checkpoint/quyền/nguồn vận hành, T05 mới triển khai đọc/validator/status. Chưa có test hành vi schema tự động hoặc UI, chưa chạy đủ mười bước, không đo hiệu năng/AI. Mẫu/source/script là nguồn dùng lại, không file tạm. Biến thể tạo trong bộ nhớ nên không có thư mục tạm phải xóa; hai thư mục rỗng tồn từ T01 vẫn giữ theo ngoại lệ đã duyệt, không thử xóa lại.
