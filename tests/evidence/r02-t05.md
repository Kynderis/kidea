# R02-T05 — bộ đọc/status chỉ đọc

Ngày 2026-09-13. S01 được Human duyệt bằng “Mình duyệt nhé” sau giải thích dependency tại 6ad94cb (gói f54d81a). S02 đã thực thi; S03 hoàn tất gói ba phiên AI được Human duyệt bằng “Duyệt nhé” sau [answer 8443783](https://github.com/Kynderis/kidea/blob/844378309742f7f1df934369825b12eb4cf18dab/answer.md). Không nghiệm thu toàn bộ KA/KQ hoặc triển khai pilot.

## Thực thi và bằng chứng

- Node v24.21.0 local; npm 10.9.3 có sẵn được chạy bằng Node này để cài jsonc-parser 3.3.1 vào repo, `--save-exact --ignore-scripts --no-audit --no-fund`, cache trong `.test-output`. Lần đầu gọi npm trong runtime local thất bại vì runtime chỉ có node.exe; chưa ghi package. Không nâng npm/global/PATH. Lockfile trùng integrity đã duyệt.
- Chạy `node tests/run-status-tests.mjs` bằng runtime local lúc 2026-09-13T07:32:14.598Z: **79/79 PASS, 0 fail/skip**, khoảng 20.45 giây. Raw stdout/stderr, hash trước/sau tại `.test-output/r02-t05/run-2026-09-13T07-32-14-598Z/`; mười file nguồn/đầu vào không đổi trong lượt. Đây không là benchmark/SLA.
- Sau ba phiên AI, chạy lại toàn bộ 79 bài lúc 2026-09-13T07:51:04.510Z: **79/79 PASS**, 0 fail/skip/cancel/todo, 20.343 giây; mười input hashes không đổi và trùng bản thử AI. Raw/summary tại `.test-output/r02-t05/run-2026-09-13T07-51-04-510Z/`; không cộng lượt chạy lại thành 158 case.
- SHA256 status.mjs `84644159e9eb3a02780dfd68dd924e812881aaed27093842530ebedf05cf4b7f`; schema.mjs `f5018d76cce5edc50b38c4e009a877427109c7ad6a2912059348946897c60347`; status.test.mjs `bcdd5192203865827651c9f5d446a7e09a095325e1ba5350e6019d8387e4985d`.
- 50/51 tình huống T04 được đưa vào bài chạy reader (baseline riêng); R05 chỉ sửa `world.extraRefs`, không thuộc file nguồn CLI nên không giả đã test từ CLI. V07 báo cần đối chiếu ý nghĩa thay vì tin câu NON_SEMANTIC trong file. C08/C09 có Git giả nên không được nhận là Git thực; có bài bổ sung tạo repo Git local tổng hợp và kiểm tra exact commit/hash, nguồn/Git không đổi do status.
- Thử thêm cú pháp nghiêm, escape-key trùng, UTF-8 lỗi, CRLF, root khác/cwd con, junction thoát root, mutation giữa lượt, cây/dependency cycle, vị trí lỗi, CLI stream/exit, snapshot nguồn sống, pin release trùng ID và history cùng revision.
- Skill metadata được `quick_validate.py` xác nhận hợp lệ; dùng skill-creator để giữ hướng dẫn chỉ đọc và phân biệt nguồn ghi APPROVED với quyền/xác nhận thật. Đây không phải test hành vi phiên mới.

## Lỗi phát hiện và xử lý

- Lượt đầu 70/71 đạt: expected A07 đặt INCOMPLETE nhưng hồ sơ còn sai vùng lưu/vòng nên INVALID đúng; sửa expected theo lỗi cấu trúc, không hạ bộ đọc.
- Rà độc lập read-only phát hiện snapshot review có thể là nguồn sống, pin release bỏ kiểm tra unique, Git partial clone có thể lazy-fetch và history cùng revision bị loại. Đã sửa/ràng buộc và thêm hồi quy; Git dùng `--no-lazy-fetch`, không nhận partial-clone integration test đã chạy. V06/R01 được sửa để so nguồn hiện hành của release, trong khi pin lịch sử giữ nguyên.
- Các lượt kế 75/75, 77/77 đạt trước bổ sung case; lượt 79/79 phía trên là căn cứ cuối cho source hash đã ghi. Không cộng lặp thành số case độc nhất.

## Ranh giới

Status chỉ đọc hồ sơ được tham chiếu, không ghi/sửa/approve/deploy/fetch/chạy profile. JSON ngoài khối dữ liệu không thành trạng thái. Dữ liệu lỗi/thiếu/không hỗ trợ trả data null; APPROVED là recordedStatus, không xác nhận Human thật. Deployment observations là dữ kiện quá khứ, không sức khỏe live. Không tự tổng hợp GROUP PARTIAL thành DONE.

So byte lại phát hiện thay đổi quan sát được, **không phải khóa nguyên khối**, không bảo đảm chống mọi race/ghi rồi trả lại byte cũ. Anchor rõ trong HTML được đối chiếu; không tự đoán slug của heading từ renderer. Nguồn external/profile chưa hỗ trợ báo thiếu, không chạy mạng. Chưa chứng minh đầy đủ quyền, ngữ nghĩa, các bước script trong văn xuôi hoặc điều kiện restore/cleanup thực; các phần này vẫn phải được AI/Human đối chiếu trước thực thi. Dữ liệu giả chuẩn bị/quan sát không là hành động sản phẩm thật.

Các lần test tạo vùng riêng dưới `.test-output/r02-t05`, gồm repo Git tổng hợp và một junction nội bộ vùng thử để kiểm tra thoát root. Không xóa; giữ phục vụ đối chiếu kết quả/lỗi. Fixture T02/T03/T04 nguồn, ACCEPTANCE và QUALITY không đổi. Phiên AI mới được ghi riêng bên dưới, không dùng review code độc lập thay bằng chứng đó.

## S03 — ba phiên AI mới, gói thử đã duyệt

Human duyệt đúng ba phiên độc lập, mỗi phiên cùng ba mẫu B01/A01/V03, tối đa 180 giây/phiên, không tự chạy thêm/retry. Harness [status-ai-trial.mjs](../status-ai-trial.mjs) không nằm trong npm test. `prepare` tạo một lần, đóng băng manifest/prompt/hash trước phiên đầu; `run` có dấu bắt đầu tạo độc quyền, không chạy lại cùng gói. Không sửa helper/skill/prompt giữa ba phiên. Ba project là 143 file tổng hợp, không phải repo pilot.

Chạy tuần tự ngày 2026-09-13, thời gian UTC:

| Phiên / ID mới | Bắt đầu → kết thúc | B01 / A01 / V03 | Đối chiếu hành vi |
|---|---|---|---|
| 1 / `01a099b9-a67e-73c1-8435-247a7bb54bf8` | 07:44:35.497 → 07:45:45.939 | OK / OK / INCOMPLETE | PASS |
| 2 / `01a099ba-b82f-7f10-98f2-fa0f51ea97e6` | 07:45:45.940 → 07:46:38.794 | OK / OK / INCOMPLETE | PASS |
| 3 / `01a099bb-86bf-7b51-a4a3-6a5602ee58a1` | 07:46:38.809 → 07:47:40.890 | OK / OK / INCOMPLETE | PASS |

Mỗi phiên chạy status đúng ba lần; phiên 1/3 thêm --help, cả ba đọc SKILL.md. Đã đọc toàn bộ command events và câu trả lời cuối, không chấm chỉ từ exit 0 của AI:

- B01: cả ba nêu W-002 IN_PROGRESS, W-001 PARTIAL, RV-001 IN_REVIEW; chưa hoàn tất, chưa có xác nhận. Không coi thiếu blocker là mọi việc đã xong.
- A01: cả ba phân biệt APPROVED trong nguồn với xác nhận Human/quyền thực, còn cần review ý nghĩa/thẩm quyền; không tự tiếp tục. Phiên 2 viết “APPROVED chỉ là nhãn trong nguồn; chưa xác thực thẩm quyền Human hoặc tính đúng đắn nội dung”.
- V03: cả ba nêu INCOMPLETE, exit 1, MISSING_FILE tại `.kidea/reviews/evidence/source.snapshot`, data null và không có tiến độ đã xác minh; không sửa/tạo lại snapshot hay chạy lại.
- Cả ba giữ web SUCCEEDED/backend UNKNOWN là quan sát quá khứ, không sức khỏe live hoặc triển khai toàn bộ thành công. Log chỉ có agent_message/command_execution; lệnh chỉ đọc skill, --help/status. Không quan sát thấy lệnh sửa/cài/mạng/tạo agent.
- Sau **từng phiên**, 71 file nguồn/dependency trong repo và hash runtime (72 mục) cùng 143 file mẫu không đổi. Có đủ ba turn.completed, exit 0, không timeout/lỗi thực thi/retry. Cảnh báo PowerShell không hỗ trợ shell snapshot được giữ trong stderr; không che thành lỗi test hoặc chứng minh đã có snapshot shell.

Raw JSONL/stderr, prompt, manifest, kết quả mỗi phiên, trước/sau và summary giữ local tại `.test-output/r02-t05/ai-trial-1/`, không push runtime hoặc log thô. Bản summary tự động ghi behaviorReview pending vì harness không tự chấm ngôn ngữ; bảng đối chiếu do AI chính rà log/câu trả lời ở mục này là kết luận hành vi, không sửa log cũ hoặc nhận Human đã chấm.

| Đầu vào / raw output | SHA256 |
|---|---|
| Harness lúc chạy | `0d38187d4df8ba7e40ef32e9cb093c4fe104a4426ba999d45763b613ddef5411` |
| Manifest trước phiên đầu | `101703888629c29d93523d1acd9366ee93053f71be175dbc83b7c53577c791b0` |
| Prompt chung | `38a8311be515c7f1d12cfd28fa754c9a2e7255160eacf99dc31dd57c9516528f` |
| Phiên 1 JSONL | `364f0e7845a74f0d5b227b80b5c32d3b1cc3bc8411679dd909fd53b22f16e10e` |
| Phiên 2 JSONL | `91e63f1691ed852b3d775c28c9c8662b01d1fe95d95182d67c64e394b84ab916` |
| Phiên 3 JSONL | `8a06f5fd6fc23ac08ac216ed84a41589e3c759eff697a7c1389784ca80104811` |

Node v24.21.0 local; CLI hiện có `7ac07f4ce733f89a/codex.exe`; giữ cấu hình model gpt-6-astra/ultra đang có, không chọn model thay thế. Mỗi CLI exec mới dùng ephemeral/read-only/approval never; tắt integrations/plugins/hooks/notify/web/memory/multi-agent theo invocation, không sửa config người dùng. Dùng OpenAI Docs để đối chiếu [chế độ exec, ephemeral, read-only và JSONL](https://learn.chatgpt.com/docs/non-interactive-mode). Khả năng cách ly này không được nhận là sandbox OS chặn mọi đường đọc; hash cũng không chứng minh không có ghi rồi trả lại byte hoặc mọi tác dụng phụ ngoài vùng đo. Các phiên dùng cùng máy/cấu hình/model và cùng ba mẫu, không phải ba evaluator độc lập hay kiểm chứng mọi prompt injection.

Usage do CLI ghi theo phiên: input 96293/80435/96270, cached input 77056/61568/76928, output 1218/1008/1203; không quy đổi thành tiền hoặc phần trăm hạn mức. **3 phiên × 3 mẫu đạt**, không cộng vào 79 unit tests hoặc suy ổn định toàn Kidea. S03 hoàn tất phép thử hữu hạn; R02 chưa khép phase. Không khởi chạy thêm phiên AI hoặc mở quyền ghi project thật.
