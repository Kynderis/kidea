# R02-T08-S02 — hồ sơ review và approval đúng bản

Ngày 2026-09-14. Phạm vi Human đã duyệt: [R02-T08-S01-r1](../../KIDEA_ROADMAP.md#approve-interface-approved). Chỉ kiểm thử xác định trên project giả, không chạy AI trial, resume, sandbox, ACL/VM, project thật hoặc pilot.

## Bản cuối

**196/196 PASS, 0 FAIL, 0 skipped, 0 cancelled.** Runner bắt đầu `2026-09-14T10:59:44.545Z`, thời gian test `147776.4039 ms`, exit 0, `inputsUnchanged: true`. Không cộng lượt thử riêng hoặc kết quả 174 của bản trước vào con số này.

| Bộ kiểm tra | Ca đạt |
|---|---:|
| Public helper/đối số | 15 |
| Status/schema/version/gate | 78 |
| Writer hợp tác/Git/cleanup | 61 |
| Init/caller | 20 |
| Review từ init thật, xác nhận/feedback/revision/Git/lỗi | 22 |

Lệnh tái lập: `.tools/node-v24.21.0-win-x64/node.exe tests/r02-t07/run-tests.mjs`. Runner theo dõi runtime scripts, skill, hai procedure init/approve, các suite/fixture dùng chung và dependency manifest trước–sau. Kiểm tra cấu trúc skill bằng `quick_validate.py` cũng đạt. Host đọc lại: Windows, D là NTFS/Fixed; Node `v24.21.0`, SHA-256 `ba4e6d110e8c1592a1ecd390f6b05f3da124b13871a5be62b341a07a853c6c32`.

Raw evidence giữ local tại `.test-output/r02-t07/regression-djjq9C/`; stdout ghi đường các fixture. Không đưa hàng loạt bản sao dữ liệu giả vào Git.

- `summary.json`: SHA-256 `0379437263d0b3a0ecfdfdf754703283bd89c2dfdca1ee02ad99605c5065d9bd`.
- `stdout.txt`: SHA-256 `d7b0afd24e0f184e08514d92725ff6159880be417f276e92631b86c8d0b60d70`.
- `stderr.txt`: rỗng, SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

## Những gì được kiểm chứng

- Public `approve` nhận request tin cậy qua stdin; giữ sáu hành động. CREATE tạo DRAFT, nối work/owner gate và giữ phiên bản nội dung/đầu vào; SUBMIT cần đánh giá điều kiện và lưu căn cứ trước trình. APPROVE chỉ ghi xác nhận rõ cho đúng ID/revision/digest/owners của bản IN_REVIEW. Không tự DRAFT → APPROVED, không dùng PASS/lời file để duyệt.
- Góp ý giải thích không thành approval; yêu cầu sửa/từ chối giữ phản hồi và đưa DRAFT. REVISE giữ bản cũ, tăng revision và không giữ confirmation ở bản hiện hành. Approval lặp trên cùng bản APPROVED không ghi thêm.
- N/A giữ purpose/lý do; không biến owner thành DONE hoặc duyệt/hoàn tất cha. Ca cha/con dùng work tree thật có child TASK, không chỉ hai owner ngang hàng. Không đổi currentItem hoặc nguồn sản phẩm.
- Nguồn review đã cũ khiến public status không có verified data. REVISE giới hạn ở review được chọn sau đối chiếu các byte nguồn hiện tại; giữ nguyên nguồn sản phẩm và approval lịch sử. Sai comparison, nguồn thiếu, record không hợp lệ, thiếu quyền, sai ID/revision/digest/owner và pending đều bị chặn. Generic writer không nhận ngoại lệ STALE.
- Thay nguồn sau prepare bị chặn trước effects; lỗi ghi dở giữ pending/evidence và chặn gọi tiếp. Không tự restore/replay/dọn marker. Hồi quy writer hiện hành bao gồm các rủi ro đường dẫn và cleanup đã có, không tuyên bố thêm bảo đảm khóa OS.
- Lịch sử review dùng fixed local Git khi exact bytes có bản giữ và được phép đọc, không đổi HEAD/index. Không có quyền đọc Git thì chặn graph có Git refs; snapshot vẫn là fallback cho dữ liệu chưa được Git giữ đúng byte. Request/permission và bản review cũ được giữ để truy vết, không coi file đó là nguồn cấp quyền.

## Lượt thử trước bản cuối

- 18 ca review ban đầu đạt trên bản đầu (`.test-output/r02-t08/review-ghGTMP/`), không thay full regression.
- Lượt mở rộng bốn ca (`.test-output/r02-t08/review-JAj0Ar/`) có một FAIL: fixture child dùng `NOT_STARTED` không thuộc schema (`TODO/IN_PROGRESS/DONE`). Đã sửa fixture sang `TODO`, không nới validator hoặc sửa expected để chấp nhận dữ liệu sai. Ca riêng sau sửa đạt (`review-K67OAH`), rồi toàn bộ 196 ca chạy lại trên bản nguồn cuối. Giữ fixture lỗi để đối chiếu.

## Giới hạn còn lại

Helper không xác thực Human, không chứng minh caller đã đọc ngữ nghĩa/đủ dependency hoặc conditionsMet là đúng; procedure yêu cầu đối chiếu thực trước ghi. Đây là kiểm chứng runtime xác định, chưa là behavioral AI acceptance hoặc hoàn tất T08/toàn lõi.

Candidate chưa có thao tác giữ approval khi chứng minh sửa thuần trình bày; nghĩa T03 vẫn giữ, không ép REVISE làm mất approval đó. Nguồn thiếu/mâu thuẫn hoặc ảnh hưởng chưa chắc phải dừng phần phụ thuộc. Resume/change/visualize, phục hồi sau ngắt, AI tích hợp và pilot chưa mở. T07 AI vẫn 0/3, không dùng quota cũ cho bản/luồng mới; sự cố launcher/ACL riêng chưa giải quyết.
