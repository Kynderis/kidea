# R02-T04 — Mẫu số 2 và đối chiếu hợp đồng, 2026-09-13

Nguồn đầu vào: [S07-r1 tại 7fd8d05](https://github.com/Kynderis/kidea/blob/7fd8d059db6b9085f13ec4ed6427126b57317880/KIDEA_DESIGN.md#schema-v2-proposal), Human “Tôi duyệt” sau answer cùng bản; các nguyên tắc T04 trước đó có căn cứ tại [tracker](../../KIDEA_ROADMAP.md#r02-t04-result). Không suy đây là kết quả Kidea runtime.

## S03 — Bộ mẫu

[Catalog](../fixtures/r02-t04/README.md) tạo 48 file ảo/6 record hiện hành trong baseline, 51 biến thể trong bộ nhớ. Có cả kiểu plan trong biến thể S07, lịch sử review và bản source/operation cố định; không đặt một project thử trên đĩa, không chạy các payload/chỉ thị giả. Không đổi mẫu T02 số 1 hoặc tình huống T03.

Mỗi biến thể giữ ID/KA, đầu vào thực do recipe tạo, expected và lý do ngay trong catalog. Bản Git trong C08/C09 chỉ là bảng byte giả; external không được truy cập. Đây là đối chiếu tĩnh/tình huống, không phải kiểm chứng Git, filesystem, cleanup hoặc triển khai thực.

## S04 — Những điểm đã rà và sửa trong mẫu

| Hợp đồng/caller | Mẫu | Kết quả rà nội dung |
|---|---|---|
| S01/S05/S07, KA-02/09 | V02–V05, S01–S09 | Bản số 2 riêng; không bỏ sai kiểu/field/mốc/khóa trùng hoặc tự đổi số. Đích bản lưu khác vị trí source lịch sử/CREATE; loại external chưa hỗ trợ không thành hợp lệ |
| T03, KA-05–08 | V01/V07/V08, A01–A09 | Bản giữ được đối chiếu byte, đổi nghĩa/format khác nhau; history không là record hiện hành thứ hai, dữ liệu giả không cấp quyền hoặc xác nhận Human |
| S02, KA-10/11 | C01–C16 | Đủ trước/sau/mixed/sửa ngoài luồng; Git đúng/sai byte; CREATE không dùng file rỗng. C04 được sửa để đích CREATE chưa có vẫn BEFORE, không báo PLANNED |
| Cleanup/G4 | C07/C11–C14 | C11 phải ghi đủ trạng thái việc/gate giả và bằng chứng kiểm tra, không chỉ có câu “đã xong”; nhóm cha vẫn chưa xong. C12 vẫn cố ý sai khi dọn giữa lượt. Bản review không được dùng thay payload tạm rồi dọn |
| S06/G6, KA-13/28 | R01–R08 | Tách nguồn thành phần, bằng chứng kiểm tra trước triển khai và quan sát vận hành. R08 tạo revision r2 chuẩn bị dở, giữ snapshot r1/lần chạy cũ thay vì sửa r1 rồi coi hợp lệ |
| Nguồn/tiến độ, KA-09/25 | B01, S07/S08 | Một currentItemId, chuyển plan không sao chép; baseline còn partial và gate chờ, không nhận đủ hồ sơ là toàn project đã xong |

Đây là kết luận rà thiết kế/recipe; không chứng minh mọi schema constraint hoặc từng expected đã được thực thi. Một số mẫu cố ý thiếu/mâu thuẫn nhiều phần để kiểm tra chặn phụ thuộc, không làm sai thành đúng bằng cách hạ chuẩn.

## Actual kiểm tra catalog

Môi trường: Windows x64, Node local 24.21.0 đã có từ T01. Lệnh:

```text
./.tools/node-v24.21.0-win-x64/node.exe tests/fixtures/r02-t04/check-catalog.mjs
```

Expected: dựng đủ catalog, ID duy nhất, baseline Ref/snapshot byte đúng, trường hợp CREATE/lịch sử không ép source hiện tại giống snapshot, recipe Git/cleanup/retry/quan sát nhất quán và không đổi file catalog trên đĩa. Actual exit 0: 48 file ảo, 6 baseline envelope, 44 lượt kiểm tra Ref local (bao gồm đích snapshot, không phải 44 Ref duy nhất), 30 VersionRef; 2 source lịch sử/dự định khác byte hiện tại, 1 source CREATE chưa tồn tại. 51 biến thể dựng được: B 1, V 8, A 9, C 16, R 8, S 9.

`fixtureDiskUnchanged: true`, `kideaRuntimeRun: false`, `semanticExpectedExecuted: false`. Fingerprint baseline world: `c9c48e45bfa11d0df9a8d0013f64a25f8ddc08badbc8a5f1e5b178d0b515a83c`. Đây là hash dữ liệu fixture tuần tự hóa, không định dạng hash manifest sản phẩm mới.

SHA-256 byte local của hai script khi kiểm tra: catalog.mjs `5cf9261558abeb89cb64967f4482748e7e24054f546425ccec5a07374e28543b`; check-catalog.mjs `a4fd808f4b3e898b1bbed6e3936cc6ecef73bc3a39c413056570807bb4f87923`. Newline checkout có thể đổi hash script; byte virtual snapshot được định nghĩa rõ trong chuỗi và tính trực tiếp, không tự chuẩn hóa file sản phẩm.

Hồi quy khung: `./.tools/node-v24.21.0-win-x64/node.exe --test tests/helper.test.mjs` — 16/16 pass, 0 fail/skip. Không đổi helper/skill/test khung; sáu action vẫn chưa triển khai. Không tính kết quả này thành 51 test Kidea hoặc nghiệm thu KA/KQ.

## Giới hạn và cleanup

Không chạy phiên AI mới, parser chuẩn, Git thật trên fixture, thao tác ghi/phục hồi/dọn file hoặc deploy. Các nguồn test là dữ liệu dùng lại; không tạo scratch hoặc xóa hai thư mục rỗng T01. Khép T04 ở mức hợp đồng và mẫu tĩnh; T05/T06 và các task action còn phải hiện thực/kiểm chứng. Không đổi AC/QUALITY/G2, bỏ gate hoặc dùng mock thay bằng chứng thực.
