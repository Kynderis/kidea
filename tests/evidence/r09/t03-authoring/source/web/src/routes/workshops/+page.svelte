<script lang="ts">import {scheduleLabel,observationLabel} from '$lib/display';import {resolve} from '$app/paths';import type {PageData} from './$types';let {data}:{data:PageData}=$props();</script>
<svelte:head><title>Danh sách workshop</title><meta name="description" content="Danh sách workshop thử nghiệm và lịch đang được công bố trong lab." /><link rel="canonical" href="https://workshop.test/workshops" /></svelte:head>
<h1>Danh sách workshop</h1>
{#each data.workshops as workshop (workshop.id)}<article><h2><a href={resolve('/workshops/[id]',{id:workshop.id})}>{workshop.title}</a></h2><p>{workshop.state==='OPEN'?'Mở đăng ký':'Tạm dừng'}</p><p>{scheduleLabel(workshop.schedule.start)} — {scheduleLabel(workshop.schedule.end)}</p><p>{workshop.remaining}/{workshop.capacity} chỗ còn theo lần quan sát. Không phải cam kết giữ chỗ.</p><p>Quan sát lúc {observationLabel(workshop.observedAt)} · Chưa cập nhật trực tiếp</p></article>{:else}<p>Chưa có workshop được công bố.</p>{/each}
