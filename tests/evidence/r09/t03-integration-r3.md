# R09 T03 — real backend/Web HTTPS integration

[Final15checks](t03-integration-r3/run/browser/result.json), [controller/cleanup](t03-integration-r3/run/result.json), [frozen manifest](t03-integration-r3/manifest.json), [driver snapshot](t03-integration-r3/source/browser.mjs). Backend source3876326 from completed build-r10; Web source029b663,33unit/18SSR–Chrome/check/lint/build PASS. Exact binary/build/dependency/script hashes are bound and were verified before/after integration.

Run r3 passed all15scoped checks with real C++/SQLite, Node Web and Caddy in an internal Docker network, no host ports. Chromium Linux153.0.8010.12 trusts only the run-local CA; no TLS bypass or host trust installation. The separate Web evidence covers actual Chrome on Mac Intel at360/1280 and100/200%zoom. Linux Chromium integration is not mislabeled as Mac Chrome or Apple Silicon.

Verified: JS-off intro/list/detail; authenticated server sessions and HttpOnly Secure cookies; lost reply after an actual committed POST, reload and GET of the same intent without replay; last-seat rejection; Escape without mutation and cancellation of the exact ID; rebooking with two distinct historical IDs; immutable old result; private SSR/user isolation; changed/invalid session handling; forged proxy headers/CSRF rejection; PAUSED public/UI behavior, historical result lookup and rejection of both new registration and cancellation under the pre-T09 baseline. No page script errors or page requests outside the lab origin. Screenshot of real private history was inspected.

[First run FAIL](t03-integration-r1/run/browser/result.json) is retained. Five checks had passed; the test then incorrectly looked for “Đăng ký” after cancellation even though the cached public snapshot still showed zero. The approved UI uses “Kiểm tra và đăng ký” for that state. R2 corrected the expected control, additionally verified backend remaining=1 and passed11checks. R3 added the outstanding JS-off/PAUSED coverage and passed15. No application rule, safety check or history assertion was relaxed. All three runs have separate outputs/source snapshots/manifests; application/backend artifacts are the same across them.

All owned containers and internal networks were removed; raw fake DB/logs and public certificates are retained. Generated CA private keys remain only in the local test-output directories and were not copied into Git evidence. No download, system install, public deployment or cloud resource was used.

This is the first implementation slice, not complete admin/events/ops, all137obligations, final G2, final maps or R09 acceptance. EXr2 remains a backend TSan limitation. The public replan/provenance package must be confirmed before the currently skeletal work tree can progress through its gates. T04 max2ACTIVE and T09 PAUSED cancellation have not been applied.
