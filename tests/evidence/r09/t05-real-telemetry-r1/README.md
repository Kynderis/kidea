# R09 T05 — partial real backend telemetry and primary operations Web

Technical milestone under the full R09 grant. T05 and `W-009-ADMIN-OPS` remain IN_PROGRESS; review revision 10 is DRAFT. This is not G2, Human acceptance, production readiness, independent-host survival, or completion of Kidea. The pilot repository is local and has no remote. `pilot-local-bundle.json` verifies a local ignored Git bundle of tracked source/metadata history at the checkpoint; the bundle itself is not uploaded and excludes ignored dependencies/build/sample/database files. No restore was executed and this is not independent-host or application backup proof. Product source commits and the later public checkpoint commit are recorded separately.

`final-checks.json`, `final-source.json`, `facets.md` and `pilot-checkpoint.json` describe the exact source, results and remaining work. `finalization-r3/preservation.json` proves that public REVISE/SAVE preserved other reviews, previous acceptance/history, owners, statuses, gates and blockers. A final public READ binds the committed checkpoint.

## Fresh results and limitations

- Backend compiled source `0fda8b7`, 116 source files and 824 vendor files, frozen in `backend-build-r2/manifest.json`. Four presets ran all 50 CTest cases and 856 functional assertions. Dev/ASan/release passed raw CTest. TSan raw JUnit retains seven failures and 14 SQLite–WAL reports, classified only under the already-approved EX-T02-WAL-01 r2; 43 old cases are clean. This is not an entirely clean TSan backend and adds no exception. Each preset also passed 167 additional telemetry checks, 77 update checks, 18 HTTP, eight session and two shutdown checks. Extra telemetry/update runtime stderr is empty. Clang-tidy non-user-header diagnostics remain in raw logs.
- Final Web source `48d4fc3`: offline lockfile install using the existing matching cache, sync/type/lint/build and 102 unit tests passed. `web-checks-r7` contains raw logs. Native Mac Chrome/SSR passed 40 tests on an explicit fake loopback API (`native-web-regressions-r4`, `native-final-checks-r2.json`). These tests do not prove backend or TLS behavior.
- `integration-r3` is the final full real C++/built Web/Linux Chrome run, with verified HTTPS/WSS and no TLS bypass. All 144 browser checks (132 old plus 12 new) and 27 SQL checks (22 old plus five new) passed. The original browser check names/order and old SQL checks are preserved. The observer source and full original 137-case coverage ledger are not upgraded by these counts.
- `audit-negative-proofs-r1` independently clones only this newly-owned synthetic final database. The full auditors reject an undeclared later write, undeclared later denial, a missing old observation denial and an undeclared old-phase write. The original raw database is never changed.

M1 counts all pending rows independently of the dispatch batch. Age is a conservative interval from a pre-COMMIT transaction timestamp and durable first observation, not exact commit age; useful tight bounds and the full detection/delivery budget are still unverified. M2 covers dispatcher/capacity incidents and exact-event processing failures; other business/version/permission/reconciliation producers are incomplete. M4 binds processing time to epoch/workshop/version, separately from browser receipt. The profile is opt-in and CREATE-only for new databases; existing databases retain business compatibility without migration/reset and report unavailable signals as UNKNOWN. The exact schema hash changes while business user_version remains 1.

Primary W5 is admin-only and read-only, with session/actor/epoch checks, no-store SSR, stale/timeout guards, late-response fences, visible private DOM clearing and retained known critical details through UNKNOWN. Other operational signals and admission remain UNKNOWN; `writeReady=false`. No public error-clear or replay control exists. The controlled SQL fault/repair changes only exact fake rows and the exact saved trigger in the newly-owned test DB, never real user data. Stale/fetch-loss/BFcache lifecycle events are explicitly injected, distinct from actual session revocation/cookie switching/SQL worker behavior. This does not prove a real BFcache history round trip or physical host failure. Screenshots cover 360/1280 px and 200% text; visible screenshots were inspected.

## Retained failures and fixes

- `backend-build-r1`: dev old checks passed, then two clang-tidy vector-growth diagnostics failed. Reserve the three bounded identity collections, freeze R2, and run all four presets. No old assertion or exception changed.
- `critical-latch-preimage`: actual prior Web source loses known critical detail when the later catalog is unavailable. One of eight additional cases fails. Keep the known incident and readable detail until authorized resolution; final full tests pass.
- `processing-identity-preimage`: actual prior source accepts an inner M4 success from another epoch. One of nine additional cases fails. Require current epoch and bounded canonical uint64 input; final full tests pass.
- `integration-r1`: FAIL 429 after 22 old browser checks; operations and SQL were not run. Explicit pacing after each completed independent admin case respects unchanged rate limits; no pacing inside race/confirmation/lost-response sequences and no hidden retry. Request/response timestamps/status diagnostics are retained. The exact caller/global bucket was not isolated.
- `integration-r2`: all 144 browser checks passed, but the aggregate FAIL remained because the old SQL helper assumed the observation phase was final. New operations SQL was NOT_RUN. Bound the old phase using the next actual audit row and exhaustively audit the later operations tail. Final R3 runs everything again; independent negative fixtures prove that the new boundary does not omit unexpected writes or denials.
- `finalization-r1`: an authoring helper misread immutable history version records as direct file refs and stopped before any public call; review remained revision 9 DRAFT. Public REVISE in `finalization-r2` succeeded, then the caller incorrectly assumed historyRefs was cumulative and stopped. `finalization-r3` verifies the byte-exact complete prior review and its nested immutable history, reuses that completed REVISE and performs only READ/SAVE. This was not a Kidea runtime or application failure.
- Earlier offline-cache, type/lint, native runner-path and wrong-working-directory authoring failures remain in their own logs and attempt summaries. They are distinguished from application defects and are never presented as product passes.

## Evidence reuse, resource and publication boundaries

`kidea-core-reuse.json` verifies all 48 current runtime/skill/test/package hashes against the recovery closure. Core 293, recovery nine and prior full-scope aggregate checks are REUSED, not fresh runs. Observer 29 Mac/15 Docker Chrome remains REUSED synthetic-source component evidence; the schema-1 fallback is unchanged and is not fed fabricated complete telemetry. `coverage-preservation.json` verifies the original 137 ledger/expected bytes; prior 11 covered/88 partial/38 NOT_RUN is REUSED, not a fresh complete audit. New verified facets and missing parts are kept separately.

The existing Node 24 runtime was selected explicitly; the machine's default Node 22 is unchanged and the >=24 project engine is unchanged. No new tool/image/dependency download, system installation, host trust change, exposed host port, cloud cost or deployment occurred. Containers use existing images, ordinary user 1000, internal networking and bounded CPU/memory/time. Owned containers/network were removed. Own isolated native test profile/cache directories also remain local, with a complete byte/mode/hash inventory; functional logs/screenshots remain publishable. Private ephemeral CA keys remain ignored; only the public root certificate is published. No Windows proof is replaced. Current Windows and Apple Silicon sources are NOT_RUN; Mac Intel PASS is not Apple Silicon PASS. Android/iOS remains unscheduled Future; R10 remains unopened.

The original cumulative 20 GiB disk baseline, minimum 100 GiB free and 128 MiB Git reserve are unchanged. Only compiler scratch created in this turn was compressed, fully verified for bytes/modes/hashes, and retired; binaries/manifests/source copies/raw logs remain. Local ignored archives and receipts retain those intermediates. Historical build runs, existing DBs and user changes were untouched. The archives are local artifacts, not portable committed build products.

Next: per-signal UNKNOWN and useful conservative age witnesses; a strictly read-only collector with least privilege and real boot/sequence/nonce/clock/freshness; then bridge the real fallback. Backup/watermark/receipt, headroom/admission/readiness, WQ, remaining functional cases and Human final gates remain. Recommended next-task model: GPT-5.6 Sol + High, based on C++ authorization/protocol/clock/regression complexity; [official model documentation](https://developers.openai.com/api/docs/models/gpt-5.6-sol). This is a task assessment, not a claim about the active model or Codex account pricing.
