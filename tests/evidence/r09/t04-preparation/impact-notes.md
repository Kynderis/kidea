# T04 preparation — approved max two ACTIVE, not applied yet

The approved D decision is in proposals/r09-next-decisions-r1.md. Apply only after the first T03 slice is verified, while admin/events/ops remain unfinished. The implementation plan already orders duplicate/max2/full. Keep the original MVP work and return point; public helper only for impact/work metadata.

Expected order: validate/auth/object access and remembered intent first; PAUSED still rejects new operations at this stage; on OPEN registration, existing ACTIVE at the same workshop returns ALREADY_REGISTERED before global quota; otherwise actor ACTIVE count>=2 returns LIMIT_REACHED before FULL. Cancellation still binds the original ID and releases quota only on an actual ACTIVE→CANCELLED change. Historical results remain immutable. Existing actors with>2 ACTIVE retain every row and can cancel; block new registrations until below2. No automatic cancellation or bulk migration.

Actual affected code: Store::registration in backend/src/store.cpp, API result enum in contracts/openapi.json, Web reply decoder/user message, backend scenario/oracle closure and Web result tests. Existing transaction serialization is already in place; the new actor-count query must remain inside it. The schema's one_active unique index is per actor/workshop and must remain; no new global check constraint that invalidates preexisting>2 rows. Assess an actor/state index for the new query without changing stored data.

Consumers requiring explicit review despite potentially no code diff: cancellation, idempotency result lookup, public capacity/version snapshot, per-workshop history, outbox/no-op rules, admin capacity/state operations, permission/epoch boundaries, backup/restore and request latency. Cross-workshop quota couples registration decisions even though public counts remain per workshop. Docs and authored responsibility rows must explain that relation.

Required new checks: zero/one/two/>two ACTIVE; CANCELLED excluded; same-workshop duplicate at quota; FULL+quota and PAUSED+quota ordering; retry after cancellation keeps old rejection; a fresh intent can succeed after cancellation; two concurrent different-workshop requests starting from one ACTIVE result in one success/one limit; cancel/register both serial orders; no lost existing rows or extra outbox for rejection. Preserve the original796 oracle assertions and add explicit new assertions; full preset/HTTP/Web regression on final source. No permission exception, new tools, cloud or deploy is implied.

Metadata prerequisite still needs resolution: the public pilot work tree currently contains ten UNEXPANDED groups and no materialized review records, despite inherited R03–R05 content approvals. Prepare a concrete provenance/replan package through public APIs; do not fabricate confirmation, import DONE from prose, or silently remove step gates. Full R09 execution authority covers preparation/implementation, not new output acceptance.
