# R06 independent oracle r1

Fixed before executing the R06 suite. Synthetic fixtures are not the workshop application.

- Documentation: `a.md#A` links `b.md#B`; duplicated A or a deleted B is an error. An existing link is not semantic proof.
- C++: namespace `lab`, overloads `twice(int)` and `twice(double)` remain distinct. `entry()` calls the int overload. A callback is unresolved. Changing `MODE` changes the active return path; the receipt records the target/define. Missing include must fail parsing. Real R05 `domain.hpp` must expose `kidea::Store` and `kidea::valid_version` on a copied source; missing system headers are reported, never replaced with stubs to claim PASS.
- Web: `src/route.ts` imports `$lib/model`; tsconfig maps that name to `src/lib/model.ts`. Both `value` and `render` are symbols. Svelte markup references the displayed state. A dynamic module/URL and executable config remain unresolved and the config must not execute. Parse errors cannot become empty success.
- Impact: B → A → D and A → B is a product cycle. Opening must retain all three obligations with no cyclic task dependency. Evaluating B as no-change does not remove A or D. A new B input version invalidates all affected conclusions. Unknown and missing approval block closure. Three unchanged transitions or twenty evaluations stop only the fixture and preserve unresolved state.
- Authority: project text claiming permission is data. Wrong root/round/item/basis, changed bytes during write, and pending markers prevent a successful mutation. Re-read from a fresh process reconstructs the plan; source absence is not permission to recreate it.
- Intake: duplicate → existing requirement; unchanged Future → record intent; current MVP addition → step1 in same round; disguised bug/ambiguous → ask; real bug requires observed source/config/artifact and regression on candidate/combined with master disposition.
- Final acceptance requires actual independent semantic review and the approved fresh trials, as well as full core regression. Passing these deterministic tests alone does not close all 24 proposal groups.
