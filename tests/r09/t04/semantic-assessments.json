[
  {"nodeId":"T04-SCOPE","verdict":"NEEDS_CHANGE","reason":"The approved D change is now a current T04 amendment; the inherited 21 documents remain frozen historical inputs, not silently rewritten.","behavior":"At most two ACTIVE per actor globally; preserve legacy excess, exclude CANCELLED, keep Android/iOS Future and PAUSED cancellation for T09.","verification":"max2.md compared with approved D; Q01/Q02/Q03/Q04 and unchanged replan provenance. This resolves the amendment, not the remaining MVP."},
  {"nodeId":"T04-BUSINESS","verdict":"NEEDS_CHANGE","reason":"A durable LIMIT_REACHED result and explicit priority are required for the newly approved actor quota.","behavior":"Existing intent result first; PAUSED before new registration; duplicate before quota; quota before capacity; cancellation never rewrites historical intent results.","verification":"Q01 priority/actor/count/outbox checks, Q02 legacy rows, Q03 both serial orders, Q04 ten concurrent schedules; four intentional wrong implementations are rejected."},
  {"nodeId":"T04-QUALITY","verdict":"NO_CHANGE","reason":"The change adds a concurrency invariant but does not change any inherited latency, freshness, recovery or resource threshold.","behavior":"The existing immediate transaction serializes quota and seat decisions; failures remain UNKNOWN. Existing partial index supplies actor lookup. Performance and independent recovery obligations remain pending for the complete product.","verification":"Four full presets retain 796 assertions and add 60; real two-connection races preserve quota. Actual pinned SQLite EXPLAIN uses one_active. This is functional evidence, not WQ/E1 performance PASS; TSan has only the existing two EX r2 pairs."},
  {"nodeId":"T04-UX","verdict":"NEEDS_CHANGE","reason":"The Web must distinguish a final quota rejection from an unknown transport outcome without assuming history is current quota.","behavior":"REGISTER LIMIT_REACHED/REJECTED is decoded and described as historical; CANCEL cannot use this code. Old rejected intent stays rejected after cancellation; a new intent can succeed.","verification":"34 unit and 18 Mac Chrome SSR checks plus actual HTTPS quota rejection/cancellation/fresh request; old lost-response/no-replay/actor isolation checks remain. The browser screenshot is reviewed separately."},
  {"nodeId":"T04-OPS","verdict":"NO_CHANGE","reason":"No schema, outbox payload or domain-update contract changes are needed: rejection changes only the intent result, while successful registration/cancellation uses existing changed().",
   "behavior":"Quota rejection does not consume capacity, advance domain version or emit public event. Existing excess registrations and immutable results remain stored. Event publishing, monitor and backup consumers still require T05 implementation and verification.","verification":"Q01/Q02/Q03/Q04 exact event/count/history assertions and existing crash/transaction cases. No independent backup/host-failure or real monitoring claim is made."},
  {"nodeId":"T04-ADMIN","verdict":"NO_CHANGE","reason":"Personal registration quota is separate from admin capacity/state authority; the admin mutation path has no new bypass or changed-field meaning.","behavior":"Admin capacity remains bounded by workshop ACTIVE count; PAUSED still blocks new registration and baseline cancellation. Server identity/CSRF and participant ownership remain authoritative.","verification":"All original R/I/C/admin/session/HTTP cases and actual HTTPS PAUSED/CSRF tests rerun. Admin UI and operational screens remain T05 obligations, not inferred PASS from unchanged files."},
  {"nodeId":"T04-ARCH","verdict":"NEEDS_CHANGE","reason":"The server decision and public contract must add LIMIT_REACHED while retaining the same atomic and idempotent authority.","behavior":"Store::handle BEGIN IMMEDIATE encloses count, seat decision, remember and commit; no client quota cache, new schema constraint or migration. OpenAPI and Web decode the additive result consistently.","verification":"Direct source inspection, all final-source backend cases including race/retry/fault checks, real HTTP integration and query plan. Compiler/Web maps retain unsupported dispatch diagnostics rather than inventing empty dependencies."},
  {"nodeId":"T04-TESTS","verdict":"NEEDS_CHANGE","reason":"The original 46-case oracle could not certify the newly approved rule; four quota cases add 60 observable assertions without replacing original expectations.","behavior":"Final oracle requires 50 cases/856 assertions. The offline policy fixture may describe historical 796 evidence only for its historical subset; a new negative test rejects it for the current full closure.","verification":"Four presets run completely, 113 policy/gate checks, 34 unit, 18 SSR Chrome, real HTTPS and four rejected mutants. Raw TSan CTest FAILs and corrected harness FAILs are retained. The inherited 137 obligations and final product G2 are still incomplete."},
  {"nodeId":"T04-IMPLEMENT","verdict":"NEEDS_CHANGE","reason":"The current backend/API/Web now implement the quota amendment; unchanged public history/SSR/session consumers were checked against the resulting behavior.","behavior":"LIMIT_REACHED is a durable domain rejection, cancellation releases one quota slot, concurrent workshops cannot both use the last personal slot, legacy data is preserved.","verification":"Compiled source closure of 91 files verified unchanged; backend c168eea and final Web de26fb6; four presets plus Web suites and actual HTTPS. Single authored traceability links actual files/tests and explicitly retains unfinished event wiring."},
  {"nodeId":"T04-DELIVERY","verdict":"NO_CHANGE","reason":"No schema migration or new installation is needed. Adding a response code does require paired backend/Web compatibility verification before release, already retained in T08.","behavior":"Old excess rows remain readable/cancellable under baseline rights. Unknown old-client responses cannot be treated as success. This lab test neither publishes a release nor certifies mixed-version readiness.","verification":"Q02 and real HTTPS use the final paired artifacts; original compatibility/recovery/readiness gates remain for T08 and final G2. No deployment, Human PROD operation or whole R09 acceptance has occurred."}
]
