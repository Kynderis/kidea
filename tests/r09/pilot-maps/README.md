# Pilot map preparation

`read.mjs OUTPUT` invokes the trusted read-only adapters on the real21 inherited product documents and current Web source. Trusted parsers are preloaded from the already locked/installed TypeScript/Svelte packages; executable project config is not run. Receipts preserve all unresolved calls, aliases, reactive expressions and external links. They are not progress records or proof of semantic coverage.

The first document map has142nodes/1052links and no structural diagnostic. Web has162nodes/93edges and237diagnostics requiring source review. Its INCOMPLETE state is intentional: an unsupported relation must not become an empty graph. Backend Clang receipt and authored responsibility mapping are not yet complete. Do not claim all three maps or T14 pass from this extraction.

`clang-child.mjs` is only a prepared bounded capture component. It needs a frozen manifest with reviewed compiler argument arrays and final source/generated-header/tool bindings before execution. It does not select compiler commands from product text, build/deploy products, or change .kidea metadata. Each translation unit is limited to60seconds/64MiB AST output; compiler diagnostics and failed captures must be retained.
